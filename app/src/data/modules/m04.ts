import type { CourseModule } from '../types';

export const m04: CourseModule = {
  id: 'm04',
  number: 4,
  title: 'Пользователи и права',
  rank: 'Пользователь',
  description:
    'Кто хозяин в системе: пользователи, группы и суперсила sudo. Научимся читать и менять права доступа rwx, управлять владельцами файлов и разберёмся со специальными битами SUID, SGID и sticky — безопасно и с пониманием.',
  lessons: [
    {
      id: 'm04-l01',
      title: 'Пользователи, группы и суперсила sudo',
      minutes: 25,
      intro:
        'Поймёте, кем вас считает система, что такое группы и почему в Ubuntu административные задачи выполняют через sudo, а не от имени root.',
      blocks: [
        {
          type: 'paragraph',
          text: 'В модуле 3 вы научили терминал мощным трюкам: потоки, конвейеры, поиск. Теперь ответим на вопрос, который до сих пор обходили стороной: **а кто вы вообще такой для системы?** Почему одни файлы вы можете менять, а при попытке тронуть другие получаете «Permission denied»?',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Linux изначально многопользовательская система — как жилой дом. У каждого жильца своя квартира (домашняя папка), свои ключи (пароль) и общие помещения (`/tmp`, системные папки). Консьерж, который может войти куда угодно, — пользователь **root**.',
        },
        { type: 'heading', text: 'Пользователи и группы' },
        {
          type: 'paragraph',
          text: 'Каждый, кто работает в системе, — **пользователь** (user) с именем и числовым идентификатором **UID**. Обычные люди получают UID начиная с 1000; всё, что меньше, — служебные учётные записи. Пользователи объединяются в **группы** (groups): группе можно дать общие права, например «всем из группы sudo можно администрировать».',
        },
        {
          type: 'code',
          title: 'Спросим систему, кто мы',
          code: `$ whoami
student
$ id
uid=1000(student) gid=1000(student) groups=1000(student),4(adm),24(cdrom),27(sudo),30(dip),46(plugdev),122(lpadmin),134(lxd),135(sambashare)
$ groups
student adm cdrom sudo dip plugdev lpadmin lxd sambashare`,
        },
        {
          type: 'paragraph',
          text: 'Обратите внимание на группу **sudo** в выводе: именно она даёт вам право выполнять административные команды. Уберите пользователя из этой группы — и sudo перестанет для него работать. Список всех пользователей хранится в файле `/etc/passwd` (да, том самом, который мы разбирали в модуле 3), а групп — в `/etc/group`.',
        },
        { type: 'heading', text: 'root и суперсила sudo' },
        {
          type: 'paragraph',
          text: '**root** (UID 0) — суперпользователь, которому разрешено абсолютно всё. В Ubuntu вход под root по умолчанию заблокирован: это защита от случайных катастроф и от зловредных программ. Вместо этого обычный пользователь временно «повышает» одну команду через **sudo** (superuser do — «сделай как суперпользователь»).',
        },
        {
          type: 'code',
          title: 'Без sudo — нельзя, с sudo — можно',
          code: `$ cat /etc/shadow
cat: /etc/shadow: Permission denied
$ sudo cat /etc/shadow | head -3
[sudo] password for student:
root:!:19450:0:99999:7:::
daemon:*:19450:0:99999:7:::
bin:*:19450:0:99999:7:::
$ sudo whoami
root`,
        },
        {
          type: 'paragraph',
          text: 'Когда sudo спрашивает пароль — вводится **ваш обычный пароль**, а не пароль root. И не пугайтесь: при вводе на экране не появляется ни букв, ни звёздочек — это нормально, пароль всё равно вводится. Несколько минут после этого sudo помнит вас и не переспрашивает.',
        },
        {
          type: 'code',
          title: 'Разумное применение sudo',
          code: `$ sudo apt update        # обновить список пакетов — нужны права root
[sudo] password for student:
Hit:1 http://archive.ubuntu.com/ubuntu jammy InRelease
Get:2 http://security.ubuntu.com/ubuntu jammy-security InRelease [110 kB]
Reading package lists... Done`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'sudo — суперсила, а с суперсилой приходит ответственность. Команда с sudo может изменить или **удалить любой файл системы**, и Linux не станет переспрашивать. Золотые правила: не добавляйте sudo «на всякий случай»; перечитайте команду перед Enter; никогда не выполняйте `sudo rm -rf` на системных путях и не копируйте sudo-команды из интернета, не поняв их.',
        },
        { type: 'heading', text: 'Создаём нового пользователя' },
        {
          type: 'paragraph',
          text: 'Рано или поздно понадобится завести ещё одну учётную запись — для члена семьи, коллеги или отдельного «полигона» для экспериментов. Новичку лучше всего подходит интерактивная команда `sudo adduser имя`: она сама создаст домашнюю папку, спросит пароль и необязательные сведения (на вопросы про ФИО и телефон можно просто нажимать Enter). Поменять пароль позже — `sudo passwd имя`. А добавить пользователя в группу — например, разрешить ему администрировать систему — поможет `sudo usermod -aG группа имя`. Результат всегда проверяйте командой `id имя`.',
        },
        {
          type: 'code',
          title: 'Новый пользователь за три команды',
          code: `$ sudo adduser oleg            # интерактивный мастер создания
Adding user \`oleg' ...
Adding new group \`oleg' (1001) ...
Adding new user \`oleg' (1001) with group \`oleg' ...
Creating home directory \`/home/oleg' ...
Copying files from \`/etc/skel' ...
New password:
Retype new password:
passwd: password updated successfully
# дальше мастер спросит ФИО, телефон и т.п. — смело жмите Enter
Is the information correct? [Y/n] Y
$ sudo usermod -aG sudo oleg     # добавляем oleg в группу sudo
$ id oleg                        # проверяем результат
uid=1001(oleg) gid=1001(oleg) groups=1001(oleg),27(sudo)`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'В команде `usermod -aG` важны **обе** буквы: `-G` задаёт список дополнительных групп, а `-a` (append — «добавить») просит **не трогать** остальные. Напишете `usermod -G sudo oleg` без `-a` — и затрёте все текущие группы пользователя, оставив только одну: человек потеряет, например, доступ к своим устройствам и sudo. Классическая ошибка новичка — проверяйте себя через `id`.',
        },
        { type: 'heading', text: 'Кто ещё живёт в системе' },
        {
          type: 'code',
          title: 'Заглянем в список жильцов',
          code: `$ cut -d: -f1,3 /etc/passwd | tail -5   # имена и UID последних записей
usbmux:121
rtkit:124
sshd:126
systemd-coredump:999
student:1000
$ getent group sudo            # кто состоит в группе sudo
sudo:x:27:student`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Пользователи с UID меньше 1000 — не люди, а «сотрудники» системы: `daemon`, `syslog`, `www-data` нужны для работы служб. Войти на рабочий стол под ними нельзя (у них оболочка `nologin` — помните урок про grep?).',
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя вслух: что показывает команда `id`? Чем root отличается от sudo? Какая группа разрешает пользователю администрировать систему?',
        },
        {
          type: 'paragraph',
          text: 'Теперь вы знаете, «кто есть кто» в Ubuntu. Следующий урок — о том, что именно можно каждому: научимся читать и менять права доступа `rwx`.',
        },
      ],
      tasks: [
        {
          title: 'Представьтесь системе',
          difficulty: 1,
          description:
            'Выполните три команды: `whoami`, `id`, `groups`. Найдите в выводе `id` свой UID и перечислите группы, в которых состоите. Убедитесь, что среди них есть `sudo` — иначе задания следующих уроков не выполнить.',
          hint: '`id` показывает всё сразу: и номер пользователя (uid), и основную группу (gid), и полный список групп.',
          solution: `$ whoami
student
$ id
uid=1000(student) gid=1000(student) groups=1000(student),4(adm),24(cdrom),27(sudo),30(dip),46(plugdev),122(lpadmin),134(lxd),135(sambashare)
$ groups
student adm cdrom sudo dip plugdev lpadmin lxd sambashare
# uid=1000 — мы первый «человеческий» пользователь системы
# группа sudo есть — администрировать можно`,
        },
        {
          title: 'Почувствуйте разницу: с sudo и без',
          difficulty: 2,
          description:
            'Прочитайте первые 3 строки файла `/etc/shadow` (там хранятся зашифрованные пароли) сначала без sudo, потом с sudo. Объясните себе, почему в первом случае отказ, а во втором — нет. Сам файл только читаем, ничего не меняем.',
          hint: 'Команда одна и та же: `head -3 /etc/shadow`. Разница — волшебное слово в начале.',
          solution: `$ head -3 /etc/shadow
head: cannot open '/etc/shadow' for reading: Permission denied
$ sudo head -3 /etc/shadow
[sudo] password for student:
root:!:19450:0:99999:7:::
daemon:*:19450:0:99999:7:::
bin:*:19450:0:99999:7:::
# /etc/shadow доступен только root: sudo временно дал нам его права`,
        },
        {
          title: 'Проверьте своё право на sudo',
          difficulty: 2,
          description:
            'Не пробуя угадывать, проверьте двумя способами, что ваш пользователь действительно входит в группу `sudo`: через `groups` и через просмотр состава группы командой `getent group sudo`.',
          hint: '`getent group имя_группы` показывает строку описания группы из /etc/group, включая список её членов.',
          solution: `$ groups | grep sudo
student adm cdrom sudo dip plugdev lpadmin lxd sambashare
$ getent group sudo
sudo:x:27:student
# 27 — номер группы (GID), student после двоеточия — её единственный член`,
        },
        {
          title: 'Составьте рейтинг пользователей по UID',
          difficulty: 3,
          description:
            'Используя навыки модуля 3, постройте конвейер, который выведет из `/etc/passwd` имена пользователей и их UID (поля 1 и 3), отсортированные по UID по возрастанию, и покажите 5 последних строк — самых «свежих» пользователей системы.',
          hint: 'Вырезать поля — `cut`. У sort есть флаги: `-t:` задаёт разделитель полей, `-k2` — сортировать по второму полю, `-n` — как по числам.',
          solution: `$ cut -d: -f1,3 /etc/passwd | sort -t: -k2 -n | tail -5
usbmux:121
rtkit:124
sshd:126
systemd-coredump:999
student:1000
# sort -t: -k2 -n: разделитель ":", ключ — 2-е поле, числовая сортировка`,
        },
      ],
      quiz: [
        {
          question: 'Что такое sudo в Ubuntu?',
          options: [
            'Отдельный секретный пользователь системы',
            'Способ выполнить одну команду с правами root, не входя в систему как root',
            'Вирусный сканер из стандартной поставки',
            'Командная оболочка вместо bash',
          ],
          correctIndex: 1,
          explanation:
            'sudo (superuser do) временно повышает права для одной команды. Постоянный вход под root в Ubuntu заблокирован — так безопаснее.',
        },
        {
          question: 'Какие UID получают обычные («человеческие») пользователи в Ubuntu?',
          options: [
            'Только 0',
            'От 1 до 99',
            'Начиная с 1000',
            'Любые отрицательные числа',
          ],
          correctIndex: 2,
          explanation:
            'UID 0 — это root, номера меньше 1000 зарезервированы под системные учётные записи служб, а первый обычный пользователь при установке получает UID 1000.',
        },
        {
          question: 'Какая группа даёт пользователю право использовать sudo?',
          options: [
            'root',
            'sudo',
            'admin-users',
            'wheel-of-fate',
          ],
          correctIndex: 1,
          explanation:
            'Членство в группе `sudo` — это и есть разрешение администрировать систему. Проверить состав группы можно командой `getent group sudo`.',
        },
        {
          question: 'Вы вводите пароль по запросу sudo, но на экране не появляется ни символов, ни звёздочек. Что происходит?',
          options: [
            'Клавиатура сломалась — нужно перезагрузиться',
            'Терминал завис — нажмите Ctrl+C',
            'sudo не принял пароль и откажет в доступе',
            'Всё нормально: пароль вводится «вслепую», так задумано для безопасности',
          ],
          correctIndex: 3,
          explanation:
            'В терминале Linux пароль не отображается вообще никак — даже звёздочками, чтобы никто не подсмотрел его длину. Просто печатайте и нажмите Enter.',
        },
      ],
    },
    {
      id: 'm04-l02',
      title: 'Права доступа: читаем и меняем (rwx, chmod)',
      minutes: 30,
      intro:
        'Научитесь свободно читать загадочные буквы rwxr-xr-- в выводе ls -l и менять права командой chmod — и буквенной, и числовой записью.',
      blocks: [
        {
          type: 'paragraph',
          text: 'В прошлом уроке мы выяснили, **кто** вы для системы. Теперь главный вопрос: **что** кому можно. Каждый файл и папка в Linux несут на себе «табличку» из десяти символов вроде `-rw-r--r--`. Сегодня вы будете читать такие таблички с первого взгляда.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Права — как табличка на двери кабинета: «директору — всё, отделу — смотреть, остальным — вход воспрещён». Только вместо директора — **владелец** файла, вместо отдела — **группа**, а остальные — все прочие пользователи.',
        },
        { type: 'heading', text: 'Читаем вывод ls -l' },
        {
          type: 'paragraph',
          text: 'Строка прав состоит из 10 позиций. Первая — тип: `-` обычный файл, `d` папка, `l` ссылка. Дальше три тройки букв: права **владельца** (user), **группы** (group) и **остальных** (others). В каждой тройке: `r` — read (читать), `w` — write (изменять), `x` — execute (запускать). Прочерк на месте буквы — права нет.',
        },
        {
          type: 'code',
          title: 'Разбираем по косточкам',
          code: `$ ls -l /etc/passwd
-rw-r--r-- 1 root root 2847 июн  5 10:12 /etc/passwd
#   │  │  │
#   │  │  └── r-- : остальным — только читать
#   │  └───── r-- : группе — только читать
#   └──────── rw- : владельцу (root) — читать и менять
$ ls -l ~/
drwxr-xr-x 2 student student 4096 июн  9 18:02 Documents
-rw-rw-r-- 1 student student  220 июн  9 18:02 notes.txt
-rwxr-xr-x 1 student student  148 июн 10 09:15 backup.sh`,
        },
        {
          type: 'table',
          headers: ['Право', 'Для файла', 'Для папки'],
          rows: [
            ['r (read)', 'читать содержимое', 'видеть список файлов внутри'],
            ['w (write)', 'изменять содержимое', 'создавать и удалять файлы внутри'],
            ['x (execute)', 'запускать как программу', 'входить в папку (cd)'],
          ],
        },
        { type: 'heading', text: 'Меняем права: chmod' },
        {
          type: 'paragraph',
          text: 'Команду `chmod` (change mode — «изменить режим») может применять владелец файла или root. Есть два языка записи. **Буквенный**: кому (`u` владелец, `g` группа, `o` остальные, `a` все) + действие (`+` дать, `-` забрать, `=` ровно такие) + права (`rwx`). Например: `chmod u+x файл` — «дать владельцу право запуска».',
        },
        {
          type: 'code',
          title: 'Буквенная запись chmod',
          code: `$ chmod u+x backup.sh        # владельцу + запуск
$ chmod go-w notes.txt       # группе и остальным - запись
$ chmod a=r secret.txt       # всем ровно одно право: чтение
$ ls -l backup.sh notes.txt secret.txt
-rwxr-xr-x 1 student student 148 июн 10 09:20 backup.sh
-rw-r--r-- 1 student student 220 июн 10 09:20 notes.txt
-r--r--r-- 1 student student  41 июн 10 09:20 secret.txt`,
        },
        {
          type: 'paragraph',
          text: '**Числовая запись** короче и любима профессионалами: `r`=4, `w`=2, `x`=1, а сумма даёт цифру тройки. `rw-` = 4+2 = **6**, `r-x` = 4+1 = **5**, `r--` = **4**. Три цифры подряд — права владельца, группы и остальных: `644` значит `rw-r--r--`, а `755` значит `rwxr-xr-x`.',
        },
        {
          type: 'code',
          title: 'Числовая запись chmod',
          code: `$ chmod 644 notes.txt        # rw-r--r-- — обычный документ
$ chmod 755 backup.sh        # rwxr-xr-x — программа/скрипт
$ chmod 600 secret.txt       # rw------- — только владельцу
$ ls -l notes.txt backup.sh secret.txt
-rw-r--r-- 1 student student 220 июн 10 09:25 notes.txt
-rwxr-xr-x 1 student student 148 июн 10 09:25 backup.sh
-rw------- 1 student student  41 июн 10 09:25 secret.txt`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Никогда не «лечите» проблемы командой `chmod 777 файл` — она открывает полный доступ **всем**, включая запись и запуск. И уж тем более не выполняйте `chmod -R` или `rm` на системных папках: рекурсивная смена прав на `/` или `/etc` убивает систему. Экспериментируйте только в домашней папке.',
        },
        { type: 'heading', text: 'Права в действии: запускаем скрипт' },
        {
          type: 'code',
          title: 'Почему ./script.sh говорит Permission denied',
          code: `$ ./backup.sh
bash: ./backup.sh: Permission denied
$ ls -l backup.sh
-rw-r--r-- 1 student student 148 июн 10 09:30 backup.sh
# буквы x нет — запускать нельзя!
$ chmod u+x backup.sh
$ ./backup.sh
Архив готов: backup.tar.gz`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Тройка чисел, которую стоит запомнить: **644** — обычные файлы, **755** — программы и папки, **600** — личные секреты (например, ключи SSH). А `777` — почти всегда признак того, что кто-то сдался и открыл всё всем.',
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя: расшифруйте вслух `-rwxr-x---`. Кому что можно? Чем `chmod 640` отличается от `chmod 604`? Что означает `x` для папки?',
        },
        {
          type: 'paragraph',
          text: 'Вы научились читать и менять права. Но у файла есть ещё и **владелец** с **группой** — те самые два имени в выводе `ls -l`. Как их менять и какие права получают новые файлы «по умолчанию» — в следующем уроке.',
        },
      ],
      tasks: [
        {
          title: 'Создайте файл и прочитайте его «табличку»',
          difficulty: 1,
          description:
            'Создайте в домашней папке файл `diary.txt` с любой строкой. Выполните `ls -l diary.txt` и расшифруйте вслух всю строку прав: тип, права владельца, группы и остальных, имя владельца и группы.',
          hint: 'Первый символ `-` — обычный файл. Дальше три тройки: rw- (владелец), r-- (группа), r-- (остальные).',
          solution: `$ echo "Сегодня я освоил chmod" > diary.txt
$ ls -l diary.txt
-rw-rw-r-- 1 student student 30 июн 10 10:01 diary.txt
# читаем: обычный файл; владелец student может читать и писать;
# группа student — читать и писать; остальные — только читать`,
        },
        {
          title: 'Сделайте скрипт исполняемым и запустите его',
          difficulty: 2,
          description:
            'Создайте файл `hello.sh` с одной строкой `echo "Привет из скрипта!"`. Попробуйте запустить его командой `./hello.sh` и получите отказ. Исправьте права и запустите снова — скрипт должен напечатать приветствие.',
          hint: 'Для запуска не хватает права `x`. Дайте его владельцу буквенной записью chmod.',
          solution: `$ echo 'echo "Привет из скрипта!"' > hello.sh
$ ./hello.sh
bash: ./hello.sh: Permission denied
$ chmod u+x hello.sh
$ ./hello.sh
Привет из скрипта!
$ ls -l hello.sh
-rwxrw-r-- 1 student student 29 июн 10 10:05 hello.sh`,
        },
        {
          title: 'Закройте личный файл от чужих глаз',
          difficulty: 2,
          description:
            'Сделайте так, чтобы файл `diary.txt` мог читать и изменять **только владелец**: ни группа, ни остальные не должны иметь никаких прав. Выполните это сначала числовой записью, затем проверьте результат и повторите ту же настройку буквенной записью на файле `hello.sh` (забрав права у группы и остальных).',
          hint: '«Только владельцу чтение и запись» — это rw-------, то есть 4+2, 0, 0. Буквенный вариант: забрать у group и others.',
          solution: `$ chmod 600 diary.txt
$ ls -l diary.txt
-rw------- 1 student student 30 июн 10 10:01 diary.txt
$ chmod go-rwx hello.sh     # то же самое буквами: у group и others забрать всё
$ ls -l hello.sh
-rwx------ 1 student student 29 июн 10 10:05 hello.sh`,
        },
        {
          title: 'Разведка: кто что может в /etc',
          difficulty: 3,
          description:
            'Найдите в `/etc` три файла с **разными** наборами прав (например, `passwd`, `shadow`, `hostname`), выведите их права командой `ls -l` одной строкой и письменно (в файл `rights-report.txt`) объясните для каждого: кто владелец, кто может читать, кто может изменять. Подумайте, почему права именно такие.',
          hint: '`ls -l` принимает несколько имён файлов сразу. Сохранить вывод в файл поможет `>` из модуля 3.',
          solution: `$ ls -l /etc/passwd /etc/shadow /etc/hostname
-rw-r--r-- 1 root root   15 июн  5 10:12 /etc/hostname
-rw-r--r-- 1 root root 2847 июн  5 10:12 /etc/passwd
-rw-r----- 1 root shadow 1712 июн  5 10:12 /etc/shadow
$ ls -l /etc/passwd /etc/shadow /etc/hostname > rights-report.txt
$ echo "passwd читают все (там нет секретов), shadow — только root
и группа shadow (там пароли), hostname — читают все" >> rights-report.txt
$ cat rights-report.txt`,
        },
      ],
      quiz: [
        {
          question: 'Что означает запись прав `-rw-r--r--`?',
          options: [
            'Папка, все могут читать и писать',
            'Обычный файл: владелец читает и пишет, группа и остальные — только читают',
            'Программа, которую могут запускать все',
            'Ссылка на другой файл',
          ],
          correctIndex: 1,
          explanation:
            'Первый `-` — обычный файл. Далее rw- (владелец: чтение+запись), r-- (группа: чтение), r-- (остальные: чтение). В числовой записи это 644.',
        },
        {
          question: 'Чему соответствует числовая запись `chmod 755`?',
          options: [
            'rwxr-xr-x',
            'rw-r--r--',
            'rwxrwxrwx',
            'r-xr-xr-x',
          ],
          correctIndex: 0,
          explanation:
            '7 = 4+2+1 = rwx (владелец), 5 = 4+1 = r-x (группа), 5 = r-x (остальные). Типичные права для программ и папок.',
        },
        {
          question: 'Что делает команда `chmod go-w notes.txt`?',
          options: [
            'Даёт группе и остальным право записи',
            'Забирает у группы и остальных право записи, права владельца не трогает',
            'Переносит файл в папку go',
            'Удаляет файл у всех, кроме владельца',
          ],
          correctIndex: 1,
          explanation:
            '`go` — это group и others, `-` — забрать право, `w` — запись. Владелец (u) в выражении не упомянут, его права остаются как были.',
        },
        {
          question: 'Что означает право `x` для ПАПКИ?',
          options: [
            'Папку можно удалить',
            'В папку можно войти командой cd',
            'Папку можно переименовать',
            'Папка сжата архиватором',
          ],
          correctIndex: 1,
          explanation:
            'Для папки x — это «вход»: без него нельзя cd внутрь и нельзя открыть файлы, даже зная их имена. Право r при этом позволяет лишь увидеть список имён.',
        },
        {
          question: 'Почему `chmod 777 report.doc` — плохая идея?',
          options: [
            'Число 777 зарезервировано для root',
            'Она открывает полный доступ (чтение, запись, запуск) вообще всем пользователям системы',
            'Файл после этого нельзя удалить',
            'Файл станет невидимым для владельца',
          ],
          correctIndex: 1,
          explanation:
            '777 = rwxrwxrwx: любой пользователь сможет прочитать, изменить и запустить ваш файл. Для документов разумный максимум — 644, для секретов — 600.',
        },
      ],
    },
    {
      id: 'm04-l03',
      title: 'Владельцы файлов: chown, chgrp, umask',
      minutes: 25,
      intro:
        'Научитесь менять владельца и группу файла командами chown и chgrp, а также управлять правами «по умолчанию» для всех новых файлов через umask.',
      blocks: [
        {
          type: 'paragraph',
          text: 'В прошлом уроке мы меняли **права** файла. Но вспомните вывод `ls -l`: там ещё два имени — владелец и группа, например `student student` или `root root`. Сегодня учимся менять и их, а заодно узнаем, почему новые файлы получают права `664`, а не какие-нибудь другие.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'У каждого файла есть «хозяин» и «рабочая группа» — как у папки с документами в офисе: подпись ответственного и отдел, которому папка доступна. Хозяина может сменить только начальник (root), а вот передать папку своему отделу сотрудник может сам.',
        },
        { type: 'heading', text: 'Кто владелец?' },
        {
          type: 'paragraph',
          text: 'Третья и четвёртая колонки в `ls -l` — это владелец и группа. Владелец обычно тот, кто создал файл. Права из первой тройки (`rw-` в наших разборах) относятся именно к нему, вторая тройка — к группе, третья — ко всем остальным. Поэтому смена владельца меняет, **кто именно** получает какие права.',
        },
        {
          type: 'code',
          title: 'Смотрим на владельцев',
          code: `$ ls -l /etc/passwd
-rw-r--r-- 1 root root 2847 июн  5 10:12 /etc/passwd
# владелец root, группа root: менять файл может только администратор
$ ls -l ~/notes.txt
-rw-rw-r-- 1 student student 220 июн  9 18:02 /home/student/notes.txt
# а здесь хозяин — вы`,
        },
        { type: 'heading', text: 'chown и chgrp' },
        {
          type: 'paragraph',
          text: '`chown` (change owner) меняет владельца, `chgrp` (change group) — группу. Важное ограничение безопасности: отдать файл **другому** владельцу может только root — иначе можно было бы подбросить компромат чужому пользователю. Поэтому перед `chown` почти всегда стоит `sudo`. Флаг `-R` применяет изменение рекурсивно — ко всей папке с содержимым.',
        },
        {
          type: 'code',
          title: 'Передаём файл из рук в руки',
          code: `$ touch shared.txt
$ ls -l shared.txt
-rw-rw-r-- 1 student student 0 июн 10 11:00 shared.txt
$ sudo chown root shared.txt        # передать файл пользователю root
[sudo] password for student:
$ ls -l shared.txt
-rw-rw-r-- 1 root student 0 июн 10 11:00 shared.txt
$ sudo chgrp sudo shared.txt        # а теперь сменить группу на sudo
$ ls -l shared.txt
-rw-rw-r-- 1 root sudo 0 июн 10 11:00 shared.txt`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Рекурсивные `sudo chown -R` и `sudo chmod -R` на чужих и системных папках — классический способ сломать систему: службы теряют доступ к своим файлам и перестают запускаться. Применяйте `-R` только внутри своей домашней папки и только понимая зачем.',
        },
        { type: 'heading', text: 'umask: права по умолчанию' },
        {
          type: 'paragraph',
          text: 'Когда вы создаёте файл, никто не спрашивает, какие ему дать права — их назначает **umask** (user mask, «маска пользователя»): число, которое **вычитается** из полного набора. В Ubuntu 22.04 umask равен `002`: из файловых `666` вычитаем `002` — получаем `664` (`rw-rw-r--`); из папочных `777` — получаем `775`. Хотите строже? Увеличьте маску.',
        },
        {
          type: 'code',
          title: 'Смотрим и меняем umask',
          code: `$ umask
0002
$ touch before.txt
$ ls -l before.txt
-rw-rw-r-- 1 student student 0 июн 10 11:10 before.txt
$ umask 0027                # строже: у остальных не будет ничего
$ touch after.txt
$ ls -l before.txt after.txt
-rw-r----- 1 student student 0 июн 10 11:12 after.txt
-rw-rw-r-- 1 student student 0 июн 10 11:10 before.txt`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'umask действует только на **новые** файлы и папки: существующие он не трогает (посмотрите на `before.txt` в примере — его права не изменились). А ещё umask живёт до конца терминальной сессии: переоткроете терминал — вернётся системное значение.',
        },
        { type: 'heading', text: 'Пример: общая папка для двоих' },
        {
          type: 'paragraph',
          text: 'Типичная задача: вы и коллега работаете над одним проектом. Рецепт: создать группу, включить в неё обоих, назначить папке эту группу через `chgrp -R` и дать группе права записи через `chmod g+w`. Всё это делается безопасно и без всяких `777`.',
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя: почему смена владельца требует sudo, а смена группы на «свою» — нет? Какие права получит новый файл при umask `0022`? Подсказка: 666 минус 022.',
        },
        {
          type: 'paragraph',
          text: 'Осталась экзотика: три специальных бита, из-за которых в выводе `ls -l` иногда появляются буквы `s` и `t`. Они — финал модуля и тема следующего урока.',
        },
      ],
      tasks: [
        {
          title: 'Узнайте, чей это файл',
          difficulty: 1,
          description:
            'Определите владельца и группу трёх файлов: `/etc/passwd`, `/etc/hostname` и любого файла из вашей домашней папки. Выпишите, у кого из них владелец не root и почему.',
          hint: 'Поможет старый знакомый `ls -l`, которому можно передать сразу несколько имён.',
          solution: `$ ls -l /etc/passwd /etc/hostname ~/notes.txt
-rw-r--r-- 1 root    root     15 июн  5 10:12 /etc/hostname
-rw-r--r-- 1 root    root   2847 июн  5 10:12 /etc/passwd
-rw-rw-r-- 1 student student  220 июн  9 18:02 /home/student/notes.txt
# системные файлы принадлежат root, домашние — вам`,
        },
        {
          title: 'Передайте файл root и попробуйте его изменить',
          difficulty: 2,
          description:
            'Создайте файл `gift.txt`, передайте его владельцу `root` **и группе `root`** командой `sudo chown root:root gift.txt`, убедитесь в смене владельца. Для чистоты эксперимента выставите права `644` (`sudo chmod 644 gift.txt`). Затем попробуйте дописать в него строку обычной командой `echo ... >>` **без sudo** — и прочитайте, что ответила система. Верните файл себе (`sudo chown student:student gift.txt`).',
          hint: 'chown умеет менять владельца и группу одним махом — формат владелец:группа. Учтите: права проверяются по порядку владелец → группа → остальные, и применяется ПЕРВАЯ подходящая категория. Поэтому если оставить группу student с правом записи, вы всё равно сможете писать в файл — группу тоже меняем на root.',
          solution: `$ echo "мой файл" > gift.txt
$ sudo chown root:root gift.txt   # и владелец, и группа — root
[sudo] password for student:
$ sudo chmod 644 gift.txt         # rw-r--r--: писать может только владелец
$ ls -l gift.txt
-rw-r--r-- 1 root root 9 июн 10 11:20 gift.txt
$ echo "ещё строка" >> gift.txt
bash: gift.txt: Permission denied
# мы не root и не в группе root → попали в «остальных», у них только r--
$ sudo chown student:student gift.txt   # забираем обратно
$ echo "ещё строка" >> gift.txt         # теперь работает
$ cat gift.txt
мой файл
ещё строка`,
        },
        {
          title: 'Наблюдайте за umask вживую',
          difficulty: 2,
          description:
            'Узнайте текущее значение umask. Создайте файл `mask-test.txt` и посмотрите его права. Затем вычислите на бумажке: 666 минус ваш umask — совпало ли с реальными правами файла?',
          hint: 'umask показывает четырёхзначное число, например 0002 — в расчётах используйте три последние цифры.',
          solution: `$ umask
0002
$ touch mask-test.txt
$ ls -l mask-test.txt
-rw-rw-r-- 1 student student 0 июн 10 11:25 mask-test.txt
# 666 - 002 = 664, то есть rw-rw-r-- — всё сошлось`,
        },
        {
          title: 'Настройте «параноидальный» режим для сессии',
          difficulty: 3,
          description:
            'Установите в текущем терминале umask `0027`. Создайте файл `private.txt` и папку `private-dir`. Объясните получившиеся права: какими они стали и почему. Проверьте, что файл из первого задания (`mask-test.txt`) свои права при этом НЕ поменял.',
          hint: 'Файл: 666 - 027 = 640 (rw-r-----). Папка: 777 - 027 = 750 (rwxr-x---). umask не действует задним числом.',
          solution: `$ umask 0027
$ touch private.txt
$ mkdir private-dir
$ ls -ld private.txt private-dir mask-test.txt
drwxr-x--- 2 student student 4096 июн 10 11:30 private-dir
-rw-r----- 1 student student    0 июн 10 11:30 private.txt
-rw-rw-r-- 1 student student    0 июн 10 11:25 mask-test.txt
# файл: 640 (владелец rw, группа r, остальные ничего)
# папка: 750; старый mask-test.txt остался rw-rw-r--`,
        },
      ],
      quiz: [
        {
          question: 'Кто может сменить владельца файла на другого пользователя?',
          options: [
            'Любой, кто может прочитать файл',
            'Только владелец файла, без всякого sudo',
            'Только root (на практике — через sudo)',
            'Любой член группы файла',
          ],
          correctIndex: 2,
          explanation:
            'chown на чужое имя разрешён только суперпользователю: иначе можно было бы подбрасывать файлы другим пользователям без их ведома.',
        },
        {
          question: 'Что делает команда `sudo chgrp editors report.txt`?',
          options: [
            'Меняет владельца файла на editors',
            'Меняет группу файла на editors',
            'Проверяет, входит ли editors в список групп',
            'Создаёт группу editors и кладёт туда файл',
          ],
          correctIndex: 1,
          explanation:
            'chgrp (change group) меняет только группу-владельца. Пользователя-владельца меняет chown; создать группу можно командой groupadd, но не через chgrp.',
        },
        {
          question: 'При umask `0022` какие права получит новый обычный файл?',
          options: [
            '777',
            '644 (rw-r--r--)',
            '600 (rw-------)',
            '022',
          ],
          correctIndex: 1,
          explanation:
            'База для файлов — 666; вычитаем маску 022: 666 - 022 = 644. Владелец читает и пишет, группа и остальные — только читают.',
        },
        {
          question: 'Вы установили umask 0077. Что произойдёт с файлами, созданными ДО этого?',
          options: [
            'Их права автоматически ужесточатся до 600',
            'Они будут удалены при следующем входе',
            'Ничего: umask влияет только на файлы, создаваемые после его установки',
            'Они станут принадлежать root',
          ],
          correctIndex: 2,
          explanation:
            'umask — настройка «на будущее»: она применяется в момент создания файла. Права существующих файлов меняются только явно, через chmod.',
        },
      ],
    },
    {
      id: 'm04-l04',
      title: 'Специальные права: SUID, SGID, sticky bit',
      minutes: 25,
      intro:
        'Разберёте три «особых полномочия» — SUID, SGID и sticky bit: узнаете, почему вы можете сменить собственный пароль, хотя файл с паролями принадлежит root, и почему в /tmp никто не может удалить чужие файлы.',
      blocks: [
        {
          type: 'paragraph',
          text: 'Вот загадка напоследок. Файл `/etc/shadow` с зашифрованными паролями доступен только root — мы видели это в первом уроке модуля. Тогда каким образом команда `passwd`, запущенная обычным пользователем, **меняет** этот файл? Ответ — в специальных битах прав, финальной теме модуля.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Специальные биты — это «нагрудный значок» для отдельных программ и папок. Обычная программа работает с правами того, кто её запустил. Программа со «значком» SUID работает с правами **своего владельца** — как курьер с пропуском директора.',
        },
        { type: 'heading', text: 'SUID: запуск от имени владельца' },
        {
          type: 'paragraph',
          text: '**SUID** (Set User ID) — бит на исполняемом файле: пока программа работает, она получает права **владельца файла**, а не запустившего. В `ls -l` он виден как буква `s` на месте `x` в тройке владельца. Так `passwd` (владелец — root) может записать новый пароль в `/etc/shadow` за вас.',
        },
        {
          type: 'code',
          title: 'Носители значка SUID',
          code: `$ ls -l /usr/bin/passwd
-rwsr-xr-x 1 root root 68208 мар 23  2022 /usr/bin/passwd
#     ↑ s вместо x у владельца: запускается с правами root
$ ls -l /usr/bin/chsh /usr/bin/crontab /usr/bin/newgrp /usr/bin/sudo
-rwsr-xr-x 1 root root    88464 фев  7  2022 /usr/bin/chsh
-rwxr-sr-x 1 root crontab  43568 фев 14  2022 /usr/bin/crontab
-rwsr-xr-x 1 root root    44760 фев  7  2022 /usr/bin/newgrp
-rwsr-xr-x 1 root root   232416 апр  4  2023 /usr/bin/sudo
# ls выводит файлы по алфавиту; у crontab буква s в тройке ГРУППЫ —
# это SGID, о нём поговорим ниже`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'SUID — частая цель атак, поэтому правила железные: никогда не ставьте SUID на свои скрипты и программы «для удобства» (для скриптов Linux этот бит вообще игнорирует), а чужой файл с SUID-битом в домашней папке — повод насторожиться. SUID оправдан только у проверенных системных утилит вроде `passwd` и `sudo`.',
        },
        { type: 'heading', text: 'SGID и sticky bit' },
        {
          type: 'paragraph',
          text: '**SGID** (Set Group ID, цифра 2) работает похоже, но с группой. Его лучшее применение — на папке: файлы, созданные внутри, **наследуют группу папки**, а не личную группу автора. Идеально для общих проектных папок. **Sticky bit** («липкий» бит, цифра 1) на папке означает: удалить или переименовать файл внутри может только его владелец — даже если папка открыта всем. Именно так устроена `/tmp`.',
        },
        {
          type: 'code',
          title: 'Смотрим на SGID и sticky',
          code: `$ ls -ld /tmp
drwxrwxrwt 15 root root 4096 июн 10 12:00 /tmp
#        ↑ t на месте x у «остальных» — это sticky bit
$ mkdir shared && chmod 2775 shared     # включаем SGID на своей папке
$ ls -ld shared
drwxrwsr-x 2 student student 4096 июн 10 12:01 shared
#        ↑ s в тройке группы — SGID включён
$ touch shared/test.txt
$ ls -l shared/test.txt
-rw-rw-r-- 1 student student 0 июн 10 12:02 shared/test.txt
# группа унаследована от папки — а не личная группа пользователя`,
        },
        {
          type: 'paragraph',
          text: 'В числовой записи chmod специальные биты — **первая** цифра: 4 = SUID, 2 = SGID, 1 = sticky. Поэтому `chmod 1777` — это «rwxrwxrwt» (так устроена /tmp), `chmod 2755` — «rwxr-sr-x», `chmod 4755` — «rwsr-xr-x». Найти все SUID-программы системы можно знакомым `find`:',
        },
        {
          type: 'code',
          title: 'Аудит: ищем все SUID-программы',
          code: `$ find /usr/bin -perm -4000 | head -6    # -perm -4000: бит SUID установлен
/usr/bin/sudo
/usr/bin/passwd
/usr/bin/chsh
/usr/bin/chfn
/usr/bin/newgrp
/usr/bin/gpasswd`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Буквы-признаки в `ls -l`: `s` в тройке владельца — SUID, `s` в тройке группы — SGID, `t` в тройке остальных — sticky bit. Видите такое на системной утилите — это нормально; на чужом файле в `/tmp` — странно.',
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя: почему `passwd` может писать в `/etc/shadow`? Что защищает sticky bit на `/tmp`? Расшифруйте права `rwxr-sr-x` в числах (подсказка: 2 7 5 5).',
        },
        { type: 'heading', text: 'Что дальше' },
        {
          type: 'paragraph',
          text: 'Модуль «Пользователи и права» пройден: вы знаете, кто вы для системы, читаете права с первого взгляда, меняете их осознанно и понимаете даже специальные биты. Впереди ранг «Уверенный» и модуль 5 «Управление программами»: научимся ставить, обновлять и удалять программы через `apt`, разберёмся с репозиториями и PPA — и `sudo` из этого модуля станет вашей ежедневной командой.',
        },
      ],
      tasks: [
        {
          title: 'Найдите «значок» у passwd',
          difficulty: 1,
          description:
            'Посмотрите права файла `/usr/bin/passwd` и найдите в выводе букву `s`. Затем тем же способом проверьте `/usr/bin/sudo` и `/bin/ls` — у кого из них SUID есть, а у кого нет? Подумайте, почему `ls` он не нужен.',
          hint: 'SUID виден в первой тройке прав (у владельца) на месте буквы x.',
          solution: `$ ls -l /usr/bin/passwd
-rwsr-xr-x 1 root root 68208 мар 23  2022 /usr/bin/passwd
$ ls -l /usr/bin/sudo
-rwsr-xr-x 1 root root 232416 апр  4  2023 /usr/bin/sudo
$ ls -l /bin/ls
-rwxr-xr-x 1 root root 142144 мар 23  2022 /bin/ls
# у ls обычный x: читать список файлов можно и со своими правами`,
        },
        {
          title: 'Проверьте липкость /tmp',
          difficulty: 2,
          description:
            'Посмотрите права папки `/tmp` командой `ls -ld /tmp` и найдите букву `t`. Затем создайте там файл `my-tmp-file.txt` и убедитесь, что он ваш (`ls -l`). Объясните вслух: сможет ли другой пользователь удалить этот файл из /tmp?',
          hint: 'Нет: sticky bit разрешает удалять файлы в папке только их владельцам (и root), несмотря на права rwx для всех.',
          solution: `$ ls -ld /tmp
drwxrwxrwt 15 root root 4096 июн 10 12:10 /tmp
$ touch /tmp/my-tmp-file.txt
$ ls -l /tmp/my-tmp-file.txt
-rw-rw-r-- 1 student student 0 июн 10 12:11 /tmp/my-tmp-file.txt
# буква t = sticky bit: чужие файлы в /tmp удалить нельзя,
# хотя создавать там свои файлы могут все`,
        },
        {
          title: 'Сделайте папку с наследованием группы',
          difficulty: 2,
          description:
            'Создайте папку `teamdir`, включите на ней SGID командой `chmod 2775 teamdir`, проверьте, что в выводе `ls -ld` появилась буква `s` в тройке группы. Создайте внутри файл и убедитесь, что он унаследовал группу папки.',
          hint: 'Первая цифра 2 — это SGID. Без неё файл получил бы вашу личную группу, а не группу папки.',
          solution: `$ mkdir teamdir
$ chmod 2775 teamdir
$ ls -ld teamdir
drwxrwsr-x 2 student student 4096 июн 10 12:15 teamdir
$ touch teamdir/draft.txt
$ ls -l teamdir/draft.txt
-rw-rw-r-- 1 student student 0 июн 10 12:16 teamdir/draft.txt
# в реальной команде группу папки заранее меняют через chgrp —
# тогда у новых файлов она наследуется автоматически`,
        },
        {
          title: 'Проведите аудит SUID-программ',
          difficulty: 3,
          description:
            'Найдите все программы с битом SUID в `/usr/bin` и `/usr/sbin`, отсортируйте список по алфавиту и сохраните в файл `suid-audit.txt`. Посчитайте, сколько их, и выведите это число. Задание объединяет всё: find, sort, перенаправление, wc.',
          hint: 'Критерий `-perm -4000` у find означает «бит SUID установлен». Два каталога можно передать find одной командой.',
          solution: `$ find /usr/bin /usr/sbin -perm -4000 | sort > suid-audit.txt
$ head -5 suid-audit.txt
/usr/bin/chfn
/usr/bin/chsh
/usr/bin/gpasswd
/usr/bin/mount
/usr/bin/newgrp
$ wc -l < suid-audit.txt
18
# у вас число может отличаться — зависит от установленных пакетов`,
        },
      ],
      quiz: [
        {
          question: 'Что делает бит SUID на исполняемом файле?',
          options: [
            'Запрещает запуск файла кому-либо, кроме root',
            'Программа при запуске получает права владельца файла, а не запустившего пользователя',
            'Файл нельзя удалить',
            'Программа запускается в два раза быстрее',
          ],
          correctIndex: 1,
          explanation:
            'SUID (Set User ID) временно даёт программе права её владельца. Поэтому passwd (владелец root) может изменить /etc/shadow, даже когда её запускает обычный пользователь.',
        },
        {
          question: 'Что означает буква `t` в правах `drwxrwxrwt` у папки /tmp?',
          options: [
            'Папка временная (temporary) и очищается каждый час',
            'Папка зашифрована (transformed)',
            'Sticky bit: удалять файлы внутри могут только их владельцы',
            'Папка доступна только по расписанию (timer)',
          ],
          correctIndex: 2,
          explanation:
            'Sticky bit на папке запрещает удалять и переименовывать чужие файлы, даже если сама папка открыта всем на запись. Именно поэтому /tmp безопасна для совместного использования.',
        },
        {
          question: 'Какая команда включит SGID на папке teamdir, оставив права rwxrwxr-x?',
          options: [
            'chmod 1775 teamdir',
            'chmod 2775 teamdir',
            'chmod 4775 teamdir',
            'chmod 775 teamdir --sgid',
          ],
          correctIndex: 1,
          explanation:
            'Первая цифра в числовой записи — специальные биты: 4 = SUID, 2 = SGID, 1 = sticky. Значит SGID плюс 775 — это 2775. Флага --sgid у chmod не существует.',
        },
        {
          question: 'Зачем на общей проектной папке включают SGID?',
          options: [
            'Чтобы папку нельзя было удалить',
            'Чтобы новые файлы внутри наследовали группу папки, а не личную группу автора',
            'Чтобы папка занимала меньше места на диске',
            'Чтобы все файлы внутри стали исполняемыми',
          ],
          correctIndex: 1,
          explanation:
            'SGID на папке заставляет новые файлы наследовать её группу. Коллеги из этой группы сразу получают доступ к файлам друг друга без ручного chgrp.',
        },
      ],
    },
  ],
};
