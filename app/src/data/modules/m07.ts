import type { CourseModule } from '../types';

export const m07: CourseModule = {
  id: 'm07',
  number: 7,
  title: 'Сеть и удалённый доступ',
  rank: 'Администратор',
  description:
    'Сеть без магии: разбираемся, как компьютеры находят друг друга, учимся диагностировать проблемы, настраиваем адреса через netplan и подключаемся к удалённым машинам по SSH — с ключами, а не паролями.',
  lessons: [
    {
      id: 'm07-l01',
      title: 'Сеть простыми словами: IP-адреса, DNS, шлюз',
      minutes: 25,
      intro:
        'Поймёте, как компьютер находит сайт по имени и что означают числа вроде 192.168.1.50. Это фундамент: без него все следующие уроки о сети превратились бы в заклинания.',
      blocks: [
        {
          type: 'paragraph',
          text: 'В прошлом модуле вы научились следить за процессами, памятью и дисками — то есть за тем, что происходит внутри компьютера. Теперь выходим наружу: почти ни один компьютер сегодня не живёт в одиночестве, и администратор обязан понимать, как машины разговаривают друг с другом.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Сеть — это почта. У каждого компьютера есть адрес (IP), письма-пакеты ходят через почтовые отделения (маршрутизаторы), а чтобы не запоминать адреса наизусть, существует телефонная книга (DNS). Весь урок — разбор этой аналогии.',
        },
        {
          type: 'heading',
          text: 'IP-адрес: адрес компьютера в сети',
        },
        {
          type: 'paragraph',
          text: '**IP-адрес** — уникальный номер компьютера в сети, например `192.168.1.50`. Четыре числа от 0 до 255, разделённые точками — это формат IPv4, самый распространённый до сих пор. Когда ваш браузер загружает страницу, пакеты с данными идут именно по таким адресам, как письма по конвертам.',
        },
        {
          type: 'paragraph',
          text: 'Рядом с адресом вы часто увидите запись вида `/24` — это **маска подсети**. Она говорит, какая часть адреса — «название улицы» (сеть), а какая — «номер дома» (конкретный компьютер). Маска `/24` означает: первые три числа — сеть, последнее — компьютер. Значит, `192.168.1.50` и `192.168.1.51` — соседи в одной сети, а `192.168.2.50` — уже другая «улица».',
        },
        {
          type: 'code',
          title: 'Смотрим, какие адреса есть у нашего компьютера',
          code: `$ ip address show
1: lo: <LOOPBACK,UP,LOWER_UP> mtu 65536 state UNKNOWN
    inet 127.0.0.1/8 scope host lo
2: eth0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 state UP
    inet 192.168.1.50/24 brd 192.168.1.255 scope global eth0`,
        },
        {
          type: 'paragraph',
          text: 'Здесь два интерфейса: `lo` (loopback, «петля») с адресом `127.0.0.1` — это «я сам», и настоящая сетевая карта `eth0` с адресом `192.168.1.50/24`, выданным домашним роутером.',
        },
        {
          type: 'list',
          items: [
            '**127.0.0.1** — «я сам», петля loopback, работает всегда, даже без сети',
            '**192.168.x.x, 10.x.x.x, 172.16–31.x.x** — частные адреса для домашних и офисных сетей, в интернет напрямую не выходят',
            '**Публичный IP** — адрес, под которым вас видит интернет; обычно он один на весь дом и «живёт» на роутере',
          ],
        },
        {
          type: 'heading',
          text: 'Шлюз: дверь из локальной сети',
        },
        {
          type: 'paragraph',
          text: 'Компьютеры из одной подсети общаются напрямую, как соседи по лестничной клетке. Но чтобы выйти в интернет, пакету нужна «дверь» — **шлюз по умолчанию** (default gateway). Им почти всегда является роутер: он принимает пакеты из домашней сети и переправляет их дальше провайдеру. В таблице маршрутов ниже первая строка читается как «всё, что не из нашей сети, отправляй через `192.168.1.1`», вторая — «с соседями из `192.168.1.0/24` общаемся напрямую через eth0».',
        },
        {
          type: 'code',
          title: 'Таблица маршрутов: куда отправлять пакеты',
          code: `$ ip route show
default via 192.168.1.1 dev eth0 proto dhcp metric 100
192.168.1.0/24 dev eth0 proto kernel scope link src 192.168.1.50`,
        },
        {
          type: 'heading',
          text: 'DNS: телефонная книга интернета',
        },
        {
          type: 'paragraph',
          text: 'Запоминать `142.250.74.46` вместо «google.com» никто не хочет. Поэтому существует **DNS** (Domain Name System, «система доменных имён») — распределённая телефонная книга, которая переводит человеческие имена в IP-адреса. Когда вы вводите имя сайта, компьютер сначала спрашивает у DNS-сервера его адрес — и только потом открывает соединение. В Ubuntu 22.04 за это отвечает служба `systemd-resolved`: программы спрашивают локального «переводчика» `127.0.0.53`, а он уже обращается к внешним DNS-серверам. Есть и «карманная телефонная книга» — файл `/etc/hosts`: система заглядывает в него **до** обращения к DNS. Формат простейший: `IP-адрес имя`, по одной записи на строку. Обязательная строка любой системы — `127.0.0.1 localhost`: имя localhost всегда указывает на саму машину. Так задают короткие имена компьютерам своей сети без настройки DNS — а в модуле m10 мы подменим имя сайта через `/etc/hosts`, чтобы протестировать виртуальные хосты nginx.',
        },
        {
          type: 'code',
          title: 'Переводим имена в адреса: DNS и /etc/hosts',
          code: `$ getent hosts ubuntu.com
91.189.91.43    ubuntu.com
$ resolvectl status
Global
         Protocols: -LLMNR -mDNS -DNSOverTLS DNSSEC=no/unsupported
  resolv.conf mode: stub
# в Global нет «Current DNS Server»: 127.0.0.53 — это лишь
# локальный stub-переводчик из /etc/resolv.conf, не настоящий DNS
Link 2 (eth0)
    Current Scopes: DNS
         Protocols: +DefaultRoute +LLMNR -mDNS -DNSOverTLS DNSSEC=no/unsupported
Current DNS Server: 192.168.1.1
       DNS Servers: 192.168.1.1
# апстрим-DNS (куда stub реально ходит) перечислен в секции Link
$ cat /etc/hosts
127.0.0.1       localhost
127.0.1.1       ubuntu
$ echo "192.168.1.60  myserver" | sudo tee -a /etc/hosts
192.168.1.60  myserver
$ getent hosts myserver
192.168.1.60    myserver
# имя myserver работает без всякого DNS — запись читается из /etc/hosts`,
        },
        {
          type: 'table',
          headers: ['Термин', 'Аналогия', 'Как посмотреть'],
          rows: [
            ['IP-адрес', 'Адрес дома и квартиры', '`ip address show`'],
            ['Маска /24', 'Граница между улицей и номером дома', 'та же команда, после слэша'],
            ['Шлюз', 'Дверь со двора на большую дорогу', '`ip route show`'],
            ['DNS', 'Телефонная книга: имя → адрес', '`resolvectl status`'],
            ['127.0.0.1', 'Разговор с самим собой', 'всегда на интерфейсе `lo`'],
          ],
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Цепочка любого визита на сайт: имя → DNS → IP-адрес → шлюз → интернет. Если сайт «не открывается», сбой может быть в любом звене — и в следующем уроке мы научимся проверять каждое.',
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя вслух: чем адрес `192.168.1.50/24` отличается от `192.168.2.50/24`? Что произойдёт, если шлюз `192.168.1.1` выключить? А если «сломается» DNS — что перестанет работать, а что продолжит?',
        },
        {
          type: 'paragraph',
          text: 'Теперь у вас есть карта: адреса, маски, шлюзы, DNS. В следующем уроке берём в руки инструменты сетевого доктора — `ping`, `ss`, `traceroute`, `curl` — и учимся находить, где именно порвалась связь.',
        },
      ],
      tasks: [
        {
          title: 'Найдите свой адрес',
          difficulty: 1,
          description:
            'Выполните `ip address show` и найдите: а) адрес loopback-петли; б) адрес основной сетевой карты и его маску. Запишите их в заметки — это паспорт вашей машины в сети.',
          hint: 'Интерфейс `lo` — это «я сам», а основная карта обычно называется `eth0`, `ens33` или `enp0s3`. Маска — число после слэша.',
          solution: `$ ip address show
1: lo: <LOOPBACK,UP,LOWER_UP> mtu 65536 state UNKNOWN
    inet 127.0.0.1/8 scope host lo        # а) петля — 127.0.0.1
2: eth0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 state UP
    inet 192.168.1.50/24 scope global eth0  # б) адрес 192.168.1.50, маска /24`,
        },
        {
          title: 'Найдите шлюз и DNS-сервер',
          difficulty: 2,
          description:
            'Двумя командами узнайте: какой шлюз использует ваша система по умолчанию и какой DNS-сервер обслуживает ваши запросы. Совпадает ли адрес шлюза с адресом роутера из прошлого задания?',
          hint: 'Шлюз показывает `ip route show` в строке `default via ...`, а DNS — `resolvectl status` или файл `/etc/resolv.conf`.',
          solution: `$ ip route show
default via 192.168.1.1 dev eth0 proto dhcp metric 100   # шлюз — 192.168.1.1
192.168.1.0/24 dev eth0 proto kernel scope link src 192.168.1.50
$ cat /etc/resolv.conf
nameserver 127.0.0.53      # локальный переводчик systemd-resolved
$ resolvectl dns
Global:
Link 2 (eth0): 192.168.1.1   # а внешний DNS — сам роутер`,
        },
        {
          title: 'Переведите три имени в адреса',
          difficulty: 2,
          description:
            'С помощью `getent hosts` узнайте IP-адреса трёх сайтов: `ubuntu.com`, `ya.ru` и любого сайта на ваш выбор. Попробуйте также заведомо несуществующее имя, например `getent hosts net-takogo-sayta.example` — и объясните, почему вывод пустой.',
          hint: 'Если DNS не может найти имя, `getent` просто ничего не выводит. Добавьте `echo $?` сразу после — увидите код ошибки 2.',
          solution: `$ getent hosts ubuntu.com
91.189.91.43    ubuntu.com
$ getent hosts ya.ru
77.88.44.55     ya.ru
$ getent hosts net-takogo-sayta.example
$ echo $?          # код возврата прошлой команды
2                  # 2 = "имя не найдено" — DNS не знает такого сайта`,
        },
        {
          title: 'Нарисуйте карту своей сети',
          difficulty: 3,
          description:
            'Соберите полную картину: ваш IP и маска, шлюз, DNS-сервер. Нарисуйте на бумаге схему «мой компьютер → роутер → интернет» и подпишите адреса. Затем ответьте письменно: ваш адрес `192.168.1.50/24` — частный или публичный? Кто в этой схеме выходит в интернет от вашего имени?',
          hint: 'Диапазон `192.168.x.x` — частный. Публичный адрес живёт на роутере, и именно его видят сайты, когда вы их открываете.',
          solution: `# собираем данные одной связкой:
$ ip -brief address show      # коротко: все интерфейсы и адреса
lo               UNKNOWN        127.0.0.1/8
eth0             UP             192.168.1.50/24
$ ip route show | head -n 1
default via 192.168.1.1 dev eth0 proto dhcp metric 100
$ resolvectl dns | tail -n 1
Link 2 (eth0): 192.168.1.1
# Ответы: адрес частный (192.168.x.x), в интернет от нашего имени
# выходит роутер 192.168.1.1 — он же шлюз и DNS-ретранслятор.`,
        },
      ],
      quiz: [
        {
          question: 'Что означает запись `192.168.1.50/24`?',
          options: [
            'Компьютер имеет 24 IP-адреса',
            'Адрес 192.168.1.50, а /24 — маска: первые три числа задают сеть, последнее — компьютер в ней',
            'Компьютер подключен к 24-й версии интернета',
            'Порт 24 открыт для подключений',
          ],
          correctIndex: 1,
          explanation:
            'Число после слэша — маска подсети. /24 значит, что сеть — это `192.168.1.x`, а `50` — конкретный компьютер внутри неё.',
        },
        {
          question: 'Зачем нужен шлюз по умолчанию (default gateway)?',
          options: [
            'Чтобы раздавать Wi-Fi соседям',
            'Чтобы ускорять загрузку сайтов',
            'Через него компьютер отправляет пакеты всем, кто находится за пределами локальной сети',
            'Чтобы переводить имена сайтов в IP-адреса',
          ],
          correctIndex: 2,
          explanation:
            'С «соседями» по подсети компьютер общается напрямую. Всё остальное — через шлюз, обычно это роутер. Переводом имён занимается DNS, а не шлюз.',
        },
        {
          question: 'Вы открываете `ubuntu.com` в браузере. Что происходит в первую очередь?',
          options: [
            'Компьютер спрашивает у DNS-сервера IP-адрес имени ubuntu.com',
            'Компьютер соединяется с шлюзом и спрашивает у него сайт',
            'Браузер угадывает адрес по первым буквам',
            'Компьютер отправляет пакет всем адресам интернета сразу',
          ],
          correctIndex: 0,
          explanation:
            'Сначала имя переводится в IP-адрес через DNS — без адреса отправить пакет некуда. И только потом открывается соединение через шлюз.',
        },
        {
          question: 'Что такое адрес `127.0.0.1`?',
          options: [
            'Адрес роутера в любой сети',
            'Адрес DNS-сервера провайдера',
            'Служебный адрес, который недоступен без интернета',
            'Адрес «петли»: компьютер обращается по нему к самому себе',
          ],
          correctIndex: 3,
          explanation:
            '127.0.0.1 (интерфейс `lo`, loopback) — это «я сам». Он работает всегда, даже без сетевой карты, и часто используется для локальных проверок и тестов.',
        },
      ],
    },
    {
      id: 'm07-l02',
      title: 'Диагностика сети: ip, ss, ping, traceroute, curl, wget',
      minutes: 30,
      intro:
        'Освоите набор «сетевого доктора»: шесть команд, которые отвечают на вопросы «какой у меня адрес», «жив ли собеседник», «где обрыв» и «что отвечает сервер».',
      blocks: [
        {
          type: 'paragraph',
          text: 'Когда что-то в сети не работает, новичок говорит «интернет пропал», а администратор спрашивает: какое именно звено сломалось? В этом уроке — инструменты, которые проверяют каждое звено цепочки из прошлого урока: адрес, шлюз, маршрут, DNS и сам сервис.',
        },
        {
          type: 'heading',
          text: 'Осматриваем себя: ip и ss',
        },
        {
          type: 'paragraph',
          text: 'Первый шаг диагностики — осмотр пациента. Команду `ip` вы уже видели; у неё есть удобный краткий режим `-brief`. А команда **ss** (socket statistics, «статистика сокетов») показывает, какие сетевые «двери» — порты — открыты на вашей машине. Её флаги читаются просто: `-tuln` = **t**cp, **u**dp, **l**istening (только слушающие порты), **n**umeric (числа вместо имён).',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Если IP-адрес — это адрес здания, то **порт** — номер конкретной двери в нём. У здания 65 535 дверей: на 80-й обычно сидит веб-сайт (HTTP), на 22-й — SSH, на 443-й — защищённый HTTPS. Команда `ss` — список дверей, которые сейчас открыты.',
        },
        {
          type: 'code',
          title: 'Адреса и открытые порты нашей машины',
          code: `$ ip -brief address show
lo               UNKNOWN        127.0.0.1/8
eth0             UP             192.168.1.50/24
$ ss -tuln
Netid  State   Recv-Q  Send-Q   Local Address:Port   Peer Address:Port
udp    UNCONN  0       0        127.0.0.53%lo:53     0.0.0.0:*
tcp    LISTEN  0       4096     127.0.0.53%lo:53     0.0.0.0:*   # DNS — только для своих
tcp    LISTEN  0       128      0.0.0.0:22           0.0.0.0:*   # SSH — виден из сети`,
        },
        {
          type: 'heading',
          text: 'Проверяем связь: ping и traceroute',
        },
        {
          type: 'paragraph',
          text: '**ping** — сетевая «постучите-послушайте»: отправляет крошечный запрос и ждёт ответа. Если ответ пришёл — собеседник жив и маршрут до него есть. Время ответа в миллисекундах показывает, насколько далеко машина: домашний роутер отвечает за 1 мс, сервер за океаном — за 150.',
        },
        {
          type: 'code',
          title: 'Стучимся к шлюзу и к сайту',
          code: `$ ping -c 4 192.168.1.1
PING 192.168.1.1 (192.168.1.1) 56(84) bytes of data.
64 bytes from 192.168.1.1: icmp_seq=1 ttl=64 time=0.894 ms
64 bytes from 192.168.1.1: icmp_seq=2 ttl=64 time=1.12 ms
--- 192.168.1.1 ping statistics ---
4 packets transmitted, 4 received, 0% packet loss, time 3003ms
$ ping -c 2 ubuntu.com
PING ubuntu.com (91.189.91.43) 56(84) bytes of data.
64 bytes from 91.189.91.43: icmp_seq=1 ttl=52 time=48.3 ms`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Без флага `-c` команда `ping` стучится бесконечно — остановите её сочетанием `Ctrl+C`. Это не поломка, а особенность: ping задуман как непрерывный мониторинг.',
        },
        {
          type: 'paragraph',
          text: 'Если `ping` говорит «собеседник молчит», встаёт вопрос: где обрыв? Тут помогает **traceroute** — она показывает все «почтовые отделения», через которые проходит пакет на пути к цели. В свежей Ubuntu 22.04 её нужно доустановить. Читается вывод просто: первый шаг — ваш роутер, дальше — оборудование провайдера, последний — цель; звёздочки вместо времени — нормально, некоторые узлы молчат. Если путь обрывается на первых шагах — проблема ваша, если дальше — у провайдера или у сайта.',
        },
        {
          type: 'code',
          title: 'Отслеживаем путь пакета',
          code: `$ sudo apt install traceroute -y
...
$ traceroute ubuntu.com
traceroute to ubuntu.com (91.189.91.43), 30 hops max, 60 byte packets
 1  _gateway (192.168.1.1)  1.042 ms  1.213 ms  1.389 ms
 2  10.15.0.1 (10.15.0.1)  8.511 ms  8.744 ms  9.013 ms
 3  * * *
 4  91.189.91.43 (91.189.91.43)  48.112 ms  48.402 ms  48.690 ms`,
        },
        {
          type: 'heading',
          text: 'Разговариваем с вебом: curl и wget',
        },
        {
          type: 'paragraph',
          text: 'Сервер может быть «жив» по ping, но нужный вам сайт — не отвечать: разные службы, разные порты. Проверяем конкретный сервис инструментами **curl** и **wget**. `curl` — швейцарский нож для запросов: флаг `-I` просит только заголовки ответа, не скачивая страницу. `wget` — «грузовик» для скачивания файлов целиком. В ответе смотрите на код: `200` — успех, `301/302` — переадресация, `403` — запрещено, `404` — не найдено, `500` — серверу самому плохо.',
        },
        {
          type: 'code',
          title: 'Спрашиваем у сайта только заголовки и качаем файл',
          code: `$ curl -I https://example.com
HTTP/2 200
content-type: text/html; charset=UTF-8
server: ECS (nyb/1D2E)
$ wget -q https://releases.ubuntu.com/22.04/SHA256SUMS
$ ls -lh SHA256SUMS
-rw-rw-r-- 1 student student 202 Feb 15 12:40 SHA256SUMS
$ head -n 2 SHA256SUMS
a4acfda10b18da50e2ec2ccaf554d241 *ubuntu-22.04.5-desktop-amd64.iso`,
        },
        {
          type: 'heading',
          text: 'Алгоритм: что проверять и в каком порядке',
        },
        {
          type: 'list',
          items: [
            '**1. Себя:** `ip -brief address` — есть ли адрес у сетевой карты?',
            '**2. Шлюз:** `ping -c 3 192.168.1.1` — жив ли роутер?',
            '**3. DNS:** `getent hosts ubuntu.com` — переводятся ли имена в адреса?',
            '**4. Маршрут:** `traceroute` — где именно обрывается путь?',
            '**5. Сервис:** `curl -I https://сайт` — отвечает ли конкретная служба?',
          ],
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Золотое правило диагностики: идите по цепочке от себя к цели и не пропускайте звенья — если не работает шаг 1, проверять шаг 5 бессмысленно. Смоделируйте мысленно: `ping` шлюза работает, а `ping ubuntu.com` — нет. Какие звенья под подозрением и какой командой их различить?',
        },
        {
          type: 'paragraph',
          text: 'Теперь вы умеете не только смотреть на сеть, но и лечить её. В следующем уроке перейдём от диагностики к настройке: зададим компьютеру постоянный адрес и свой DNS через **netplan** — современный инструмент Ubuntu 22.04.',
        },
      ],
      tasks: [
        {
          title: 'Осмотрите свои порты',
          difficulty: 1,
          description:
            'Выполните `ss -tuln` и найдите в выводе: а) порты, слушающие только на `127.0.0.1` или `127.0.0.53`; б) порты, доступные отовсюду (`0.0.0.0`). Чем вторые отличаются от первых с точки зрения безопасности?',
          hint: 'Адрес `127.0.0.1` — «только для своих», `0.0.0.0` — «для всех, кто сможет достучаться». Флаг `-n` показывает порты числами.',
          solution: `$ ss -tuln
Netid  State   Recv-Q  Send-Q   Local Address:Port   Peer Address:Port
udp    UNCONN  0       0        127.0.0.53%lo:53     0.0.0.0:*
tcp    LISTEN  0       4096     127.0.0.53%lo:53     0.0.0.0:*   # а) только локально
tcp    LISTEN  0       128      0.0.0.0:22           0.0.0.0:*   # б) виден из сети
# Ответ: порт 22 (SSH) принимает подключения извне — за такими
# портами нужен глаз да глаз (этим займёмся в модуле 8).`,
        },
        {
          title: 'Проверьте три звена цепочки',
          difficulty: 2,
          description:
            'Выполните полную мини-диагностику: 1) `ip -brief address`, 2) `ping -c 3` вашего шлюза, 3) `ping -c 3 ya.ru`. Все три шага прошли? Если какой-то нет — объясните, что это означает по алгоритму из урока.',
          hint: 'Адрес шлюза смотрите в `ip route show` после слов `default via`. Не забудьте `-c 3`, иначе ping придётся останавливать через Ctrl+C.',
          solution: `$ ip -brief address show
eth0             UP             192.168.1.50/24     # 1) адрес есть
$ ip route show | head -n 1
default via 192.168.1.1 dev eth0 proto dhcp metric 100
$ ping -c 3 192.168.1.1
3 packets transmitted, 3 received, 0% packet loss  # 2) шлюз жив
$ ping -c 3 ya.ru
3 packets transmitted, 3 received, 0% packet loss  # 3) DNS и интернет в порядке`,
        },
        {
          title: 'Сравните два сайта через curl',
          difficulty: 2,
          description:
            'Выполните `curl -I http://github.com` (обратите внимание: http, не https). Какой код ответа вы получили и куда ведёт строка `Location:`? Объясните, зачем сервер так делает. Затем выполните `curl -I https://github.com` и сравните код с первым, а напоследок `curl -I https://github.com/net-stranitsa` — расшифруйте и его.',
          hint: 'Незащищённый http на современных сайтах перенаправляет на https — ищите код 301 и строку `Location:`. Несуществующая страница вернёт 404.',
          solution: `$ curl -I http://github.com
HTTP/1.1 301 Moved Permanently      # переадресация...
Location: https://github.com/       # ...на защищённую версию сайта
$ curl -I https://github.com
HTTP/2 200                          # по https — успех
$ curl -I https://github.com/net-stranitsa
HTTP/2 404                          # страницы не существует
# Ответ: сервер закрывает незашифрованный http и вежливо
# отправляет всех на https — так делают почти все современные сайты`,
        },
        {
          title: 'Детектив: найдите сломанное звено',
          difficulty: 3,
          description:
            'Представьте жалобу пользователя: «не открывается сайт ya.ru». Смоделируйте диагностику на своей машине: выполните подряд `getent hosts ya.ru`, `ping -c 2` адреса из его вывода и `curl -I https://ya.ru`. Запишите, какое звено отвечает за что, и составьте письменное заключение из трёх строк: «DNS — ...», «Маршрут — ...», «Сервис — ...».',
          hint: 'Если `getent` вернул адрес — DNS работает. Если `ping` по этому адресу проходит — маршрут есть. Если `curl` вернул любой код (даже 301) — сервис отвечает.',
          solution: `$ getent hosts ya.ru
77.88.44.55     ya.ru                        # DNS — работает
$ ping -c 2 77.88.44.55
2 packets transmitted, 2 received, 0% packet loss   # маршрут — есть
$ curl -I https://ya.ru
HTTP/2 302
location: https://dzen.ru/                   # сервис — отвечает (с редиректом)
# Заключение: все три звена в порядке, сайт доступен.`,
        },
      ],
      quiz: [
        {
          question: 'Что показывает команда `ss -tuln`?',
          options: [
            'Скорость сетевого соединения в реальном времени',
            'Список открытых (слушающих) TCP- и UDP-портов системы в числовом виде',
            'Маршрут пакета до указанного сайта',
            'Заголовки ответа веб-сервера',
          ],
          correctIndex: 1,
          explanation:
            'Флаги значат: t — TCP, u — UDP, l — только слушающие сокеты, n — числа вместо имён. Это «список открытых дверей» вашей машины.',
        },
        {
          question: 'Почему в выводе `ping` обязательно указывать флаг `-c 4` в примерах?',
          options: [
            'Без `-c` команда ping не работает в Ubuntu 22.04',
            'Флаг `-c` включает цветной вывод',
            'Иначе ping будет отправлять запросы бесконечно, пока его не остановят через Ctrl+C',
            'Флаг `-c` шифрует запросы',
          ],
          correctIndex: 2,
          explanation:
            'ping задуман как непрерывный мониторинг: без ограничения `-c` (count, «число») он стучится вечно. Останавливается сочетанием Ctrl+C.',
        },
        {
          question: '`ping 192.168.1.1` (шлюз) проходит, а `ping ubuntu.com` — нет. Где искать проблему в первую очередь?',
          options: [
            'В DNS или в маршруте за пределами шлюза: локальная сеть работает, значит сбой дальше',
            'В сетевой карте компьютера — она сломана',
            'В мониторе — сайт на самом деле открылся',
            'Нигде: если шлюз отвечает, всё остальное точно работает',
          ],
          correctIndex: 0,
          explanation:
            'Раз до шлюза пакеты ходят, локальная сеть здорова. Остаются два подозреваемых: DNS (проверить `getent hosts`) и маршрут за роутером (проверить `traceroute`).',
        },
        {
          question: 'Чем `curl -I` отличается от `wget`?',
          options: [
            'curl показывает служебные заголовки ответа сервера (не скачивая страницу), а wget скачивает файлы целиком',
            'curl работает только с FTP, wget — только с HTTP',
            'wget показывает заголовки, curl качает файлы',
            'Ничем, это два имени одной команды',
          ],
          correctIndex: 0,
          explanation:
            'Флаг `-I` у curl запрашивает только заголовки — это быстрый способ проверить «жив ли сервис и что отвечает». wget — инструмент для скачивания файлов.',
        },
      ],
    },
    {
      id: 'm07-l03',
      title: 'Настраиваем сеть: netplan и NetworkManager',
      minutes: 30,
      intro:
        'Научитесь задавать статический IP-адрес и свои DNS-серверы «по-взрослому»: через конфигурационные файлы netplan, с проверкой и откатом — не боясь потерять связь.',
      blocks: [
        {
          type: 'paragraph',
          text: 'До сих пор мы смотрели на сеть глазами диагноста. Теперь — глазами архитектора: где в Ubuntu 22.04 вообще записаны сетевые настройки и как их менять, не сломав соединение. Спойлер: думать здесь за вас умеет специальная служба, а вам остаётся написать ей «задание» в понятном формате.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Представьте стройку. **netplan** — это чертёж: документ, где описано, какой должна быть сеть. **Renderer** (NetworkManager или systemd-networkd) — бригада строителей, которая читает чертёж и делает работу. Вы общаетесь только с чертежом — бригаду выбирает система.',
        },
        {
          type: 'heading',
          text: 'Кто управляет сетью в Ubuntu 22.04',
        },
        {
          type: 'paragraph',
          text: 'В Ubuntu две «бригады». На десктопе с графическим интерфейсом сетью рулит **NetworkManager** — он же рисует значок Wi-Fi в углу экрана. На серверах без графики работает **systemd-networkd** — лёгкий и незаметный. А общий язык для обеих бригад — **netplan**: YAML-файлы в каталоге `/etc/netplan/`. Это нововведение Ubuntu, и именно оно делает настройку единообразной.',
        },
        {
          type: 'list',
          items: [
            '**netplan** — формат-чертёж: файлы `/etc/netplan/*.yaml`',
            '**NetworkManager** — бригада для десктопа (Wi-Fi, значок в трее, команда `nmcli`)',
            '**systemd-networkd** — бригада для сервера (без графики, минимум лишнего)',
            '**DHCP** — «спросить адрес у роутера автоматически»; режим по умолчанию',
          ],
        },
        {
          type: 'heading',
          text: 'Знакомимся с netplan',
        },
        {
          type: 'code',
          title: 'Смотрим текущий чертёж сети',
          code: `$ ls /etc/netplan/
01-network-manager-all.yaml
$ cat /etc/netplan/01-network-manager-all.yaml
network:
  version: 2
  renderer: NetworkManager`,
        },
        {
          type: 'paragraph',
          text: 'Это чертёж по умолчанию на десктопе: «формат версии 2, работать будет NetworkManager» — адреса раздаёт DHCP. На сервере файл подробнее: виден интерфейс и строка `dhcp4: true`. Формат **YAML** читается почти как русский текст, но вложенность в нём задаётся отступами — и только пробелами.',
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Две ловушки netplan. Первая: символ табуляции в YAML запрещён — только пробелы, иначе получите ошибку разбора. Вторая: `sudo netplan apply` применяет настройки немедленно, и если вы работаете по SSH и ошиблись — соединение порвётся. Поэтому есть команда `sudo netplan try`: она применяет конфиг **на пробу** и сама откатит его через 120 секунд, если вы не подтвердите.',
        },
        {
          type: 'heading',
          text: 'Задаём статический адрес',
        },
        {
          type: 'paragraph',
          text: 'Зачем вообще статический адрес? Для сервера DHCP — неудобен: сегодня роутер выдал ему `.50`, завтра `.51`, и все закладки сломались. Статический адрес закрепляется за машиной навсегда. Создадим новый файл конфигурации (имена файлов netplan читаются по алфавиту, поздние дополняют ранние). Чертёж ниже читается сверху вниз: интерфейсу `eth0` отключить DHCP, выдать адрес `.100` (подальше от диапазона, который раздаёт роутер), назначить шлюз и DNS — роутер плюс запасной `1.1.1.1` от Cloudflare.',
        },
        {
          type: 'code',
          title: 'Статический IP через netplan (серверный вариант)',
          code: `$ sudo nano /etc/netplan/60-static.yaml
# вставляем в файл:
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      dhcp4: false
      addresses: [192.168.1.100/24]
      routes:
        - to: default
          via: 192.168.1.1
      nameservers:
        addresses: [192.168.1.1, 1.1.1.1]
$ sudo netplan try      # пробуем: есть 120 секунд на проверку
Do you want to keep these settings?   # жмём Enter, если сеть жива`,
        },
        {
          type: 'heading',
          text: 'NetworkManager на десктопе: nmcli',
        },
        {
          type: 'paragraph',
          text: 'На десктопе править YAML вручную обычно не нужно: NetworkManager управляется мышкой через «Настройки → Сеть». Но у него есть и отличный терминальный интерфейс — **nmcli**. Им удобно смотреть состояние и быстро переключать соединения, не открывая графику.',
        },
        {
          type: 'code',
          title: 'Осматриваем сеть через nmcli',
          code: `$ nmcli device status
DEVICE   TYPE      STATE      CONNECTION
eth0     ethernet  connected  Wired connection 1
lo       loopback  unmanaged  --
$ nmcli connection show "Wired connection 1" | head -n 6
connection.id:      Wired connection 1
connection.type:    802-3-ethernet
ipv4.method:        auto
$ nmcli device show eth0 | grep IP4
IP4.ADDRESS[1]:  192.168.1.50/24
IP4.GATEWAY:     192.168.1.1
IP4.DNS[1]:      192.168.1.1`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Запомните пару: десктоп — `nmcli` и графические настройки, сервер — YAML-файлы netplan и `netplan apply`. А чертёж `/etc/netplan/*.yaml` есть в обоих мирах, просто renderer разный.',
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя: почему для сервера выбирают статический адрес вместо DHCP? Что сделает `netplan try`, если вы забыли подтвердить настройки, а SSH-соединение разорвалось?',
        },
        {
          type: 'paragraph',
          text: 'Своя сеть настроена и понятна. Финальный урок модуля — коронный: **SSH**. Вы научитесь заходить на другую машину (пока — на свою же) так, как это делают администраторы серверов по всему миру.',
        },
      ],
      tasks: [
        {
          title: 'Прочитайте чертёж своей сети',
          difficulty: 1,
          description:
            'Найдите файлы в `/etc/netplan/` и прочитайте их содержимое. Ответьте письменно: какой renderer использует ваша система — NetworkManager или networkd? Совпадает ли это с тем, что вы ожидали (десктоп/сервер)?',
          hint: 'Команды `ls /etc/netplan/` и `cat` на найденный файл. Ищите строку `renderer:` — если её нет, смотрите, есть ли графический интерфейс (значит, NetworkManager).',
          solution: `$ ls /etc/netplan/
01-network-manager-all.yaml
$ cat /etc/netplan/01-network-manager-all.yaml
network:
  version: 2
  renderer: NetworkManager
# Это десктопная Ubuntu 22.04 — renderer NetworkManager, как и ожидалось.
# На сервере (Ubuntu Server) строка была бы renderer: networkd
# или вообще отсутствовала бы (networkd используется по умолчанию).`,
        },
        {
          title: 'Осмотрите сеть через nmcli',
          difficulty: 2,
          description:
            'Тремя командами `nmcli` соберите досье: статус устройств, имя активного соединения и текущие IP/шлюз/DNS интерфейса. Сравните результат с `ip route show` — совпадают ли данные?',
          hint: 'Вам понадобятся `nmcli device status`, `nmcli connection show` и `nmcli device show <интерфейс>`. Имя соединения возьмите из первой команды, колонка CONNECTION.',
          solution: `$ nmcli device status
DEVICE   TYPE      STATE      CONNECTION
eth0     ethernet  connected  Wired connection 1
$ nmcli connection show --active
NAME                UUID                                  TYPE      DEVICE
Wired connection 1  8d2e5c1a-...                          ethernet  eth0
$ nmcli device show eth0 | grep IP4
IP4.ADDRESS[1]:  192.168.1.50/24
IP4.GATEWAY:     192.168.1.1
# Данные совпадают с ip route show — оба инструмента читают
# одну и ту же реальность, просто по-разному её подают.`,
        },
        {
          title: 'Напишите чертёж статического адреса (в черновике)',
          difficulty: 2,
          description:
            'Не трогая настоящую конфигурацию, создайте в домашней папке файл `draft-netplan.yaml` с настройками статического адреса `192.168.1.200/24` для вашего интерфейса (имя подсмотрите в `ip -brief address`), шлюзом и DNS — вашим роутером. Проверьте, что в файле нет символов табуляции.',
          hint: 'Скопируйте структуру из урока и подставьте своё имя интерфейса. Проверка на табуляцию: `grep -P "\\t" draft-netplan.yaml` — если вывод пуст, всё чисто.',
          solution: `$ ip -brief address show | grep UP
eth0             UP             192.168.1.50/24    # интерфейс — eth0
$ nano draft-netplan.yaml
# содержимое файла:
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: false
      addresses: [192.168.1.200/24]
      routes:
        - to: default
          via: 192.168.1.1
      nameservers:
        addresses: [192.168.1.1]
$ grep -P "\\t" draft-netplan.yaml    # ищем запрещённую табуляцию
$                                     # пусто — только пробелы, порядок`,
        },
        {
          title: 'Примените статический адрес с откатом',
          difficulty: 3,
          description:
            'В учебной виртуальной машине (только в ней!) перенесите свой черновик в `/etc/netplan/60-static.yaml` и примените его через `sudo netplan try`. Проверьте `ip -brief address`, что адрес сменился, и подтвердите настройки. Затем верните DHCP обратно: удалите файл и снова выполните `sudo netplan apply`.',
          hint: 'Перенос: `sudo cp draft-netplan.yaml /etc/netplan/60-static.yaml`. Если после `netplan try` сеть отвалилась — просто подождите 120 секунд, настройки откатятся сами.',
          solution: `$ sudo cp draft-netplan.yaml /etc/netplan/60-static.yaml
$ sudo netplan try
Do you want to keep these settings?    # сначала проверяем в другом окне:
$ ip -brief address show
eth0             UP             192.168.1.200/24   # адрес применился!
# возвращаемся в netplan try и жмём Enter (подтвердить)
# ...а теперь откатываемся к привычному DHCP:
$ sudo rm /etc/netplan/60-static.yaml
$ sudo netplan apply
$ ip -brief address show
eth0             UP             192.168.1.50/24    # DHCP выдал адрес снова`,
        },
      ],
      quiz: [
        {
          question: 'Что такое netplan в Ubuntu 22.04?',
          options: [
            'Программа для рисования схем сети',
            'Единый формат конфигурации (YAML в /etc/netplan/), который читают NetworkManager или systemd-networkd',
            'Антивирус для сетевого трафика',
            'Провайдер облачных серверов',
          ],
          correctIndex: 1,
          explanation:
            'netplan — это «чертёж» сети: вы пишете YAML-файл, а реальную настройку выполняет renderer — NetworkManager на десктопе или systemd-networkd на сервере.',
        },
        {
          question: 'Чем `sudo netplan try` безопаснее `sudo netplan apply`?',
          options: [
            'try работает быстрее',
            'try не требует прав sudo',
            'try шифрует конфигурацию',
            'try применяет настройки временно и автоматически откатывает их, если вы не подтвердили — спасает при обрыве SSH',
          ],
          correctIndex: 3,
          explanation:
            'netplan try даёт 120 секунд на проверку. Если сеть сломана и вы не можете подтвердить — конфигурация сама вернётся к прежней. Это страховка от потери удалённого доступа.',
        },
        {
          question: 'Почему в файлах netplan нельзя использовать табуляцию?',
          options: [
            'Можно, но только в комментариях',
            'Формат YAML в принципе не допускает символ табуляции для отступов — только пробелы',
            'Табуляция замедляет сеть',
            'netplan путает табуляцию с паролем',
          ],
          correctIndex: 1,
          explanation:
            'Это правило самого формата YAML: вложенность задаётся пробелами, табуляция вызовет ошибку разбора. Хорошие редакторы (nano, VS Code) подставляют пробелы автоматически.',
        },
        {
          question: 'Где обычно сетью управляет NetworkManager, а где systemd-networkd?',
          options: [
            'NetworkManager — на десктопе с графикой, networkd — на серверах без графики',
            'NetworkManager — только на серверах, networkd — на десктопах',
            'NetworkManager управляет Wi-Fi, networkd — Bluetooth',
            'Разницы нет, система выбирает случайно',
          ],
          correctIndex: 0,
          explanation:
            'Десктопной Ubuntu нужен значок Wi-Fi и удобное переключение — это NetworkManager. Серверу достаточно лёгкого systemd-networkd. Общий язык обоих — файлы netplan.',
        },
      ],
    },
    {
      id: 'm07-l04',
      title: 'SSH: ключи, scp, rsync, безопасное подключение',
      minutes: 35,
      intro:
        'Научитесь главному навыку администратора: управлять другим компьютером по SSH. Создадите ключи вместо пароля, скопируете файлы через scp и rsync — и всё это безопасно, на своей же машине.',
      blocks: [
        {
          type: 'paragraph',
          text: 'Настоящие серверы не стоят у администратора под столом — они в дата-центрах и облаках, иногда за тысячи километров. Управляет ими администратор из дома, и делает это через **SSH** (Secure Shell, «безопасная оболочка»). Простыми словами, SSH — это бронированная труба между двумя компьютерами: вы печатаете команды у себя, а выполняются они на том конце, и всё, что проходит по трубе (даже пароль!), зашифровано. В этом уроке мы отработаем всё на безопасном стенде: на вашей же машине, подключаясь к ней самой по адресу `localhost`.',
        },
        {
          type: 'heading',
          text: 'Первое подключение',
        },
        {
          type: 'paragraph',
          text: 'SSH состоит из двух половинок: **клиент** (команда `ssh`, в Ubuntu установлен из коробки) и **сервер** (пакет `openssh-server`, принимает подключения). Для тренировки поставим сервер на свою машину и подключимся к ней же — через петлю `127.0.0.1`, которую вы знаете как «я сам». Вопрос про «fingerprint» при первом подключении — не ошибка, а защита: сервер показывает отпечаток своего ключа, как паспорт. Отвечаем `yes`, отпечаток сохраняется в `~/.ssh/known_hosts`, и больше система не спрашивает.',
        },
        {
          type: 'code',
          title: 'Ставим сервер и подключаемся сами к себе',
          code: `$ sudo apt install -y openssh-server
...
$ ssh student@127.0.0.1
The authenticity of host '127.0.0.1' can't be established.
ED25519 key fingerprint is SHA256:k3Jx9vQ2...
Are you sure you want to continue connecting (yes/no)? yes
student@127.0.0.1's password:      # вводим свой пароль
student@ubuntu:~$ hostname         # мы "внутри" SSH-сеанса
ubuntu
student@ubuntu:~$ exit             # выходим обратно
logout`,
        },
        {
          type: 'paragraph',
          text: 'Минуточку: мы установили пакет — а сервер уже работает, никто его не запускал вручную. Знакомьтесь: **systemd**, «дирижёр» системы — самый первый процесс, который стартует при загрузке Ubuntu (его номер, PID, всегда 1). Программы, за которыми systemd приглядывает, называются **службами** (по-английски — юнитами, unit): наш `ssh`, планировщик `cron`, сетевая подсистема. Пульт управления службами — команда `systemctl`. Четыре приёма выживания: `sudo systemctl start имя` (запустить), `stop` (остановить), `restart` (перезапустить — например, после смены настроек), `status` (как она себя чувствует). А пара `enable`/`disable` решает, стартовать ли службе автоматически при загрузке.',
        },
        {
          type: 'code',
          title: 'Четыре команды выживания systemctl — на примере ssh',
          code: `$ systemctl status ssh --no-pager | head -n 3
● ssh.service - OpenBSD Secure Shell server
     Active: active (running)     # служба жива
$ sudo systemctl restart ssh      # перечитать настройки и перезапустить
$ systemctl is-enabled ssh
enabled                           # стартует сама при каждой загрузке
# автозапуск выключили бы так: sudo systemctl disable ssh`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Служба «не отвечает»? Рефлекс администратора: сначала `systemctl status имя`, затем `restart`. А если не помогло — идём читать, что служба нажаловалась в журнал: systemd ведёт дневник каждой службы, и в модуле 8 вы научитесь читать его командой `journalctl -u имя`.',
        },
        {
          type: 'heading',
          text: 'Ключи вместо пароля',
        },
        {
          type: 'paragraph',
          text: 'Пароль по SSH можно подобрать перебором — и боты в интернете делают это круглосуточно. Поэтому профессионалы используют **ключевую пару**: два файла-близнеца, созданные вместе. **Приватный ключ** (`id_ed25519`) — ваш личный, хранится у вас и никому не показывается. **Публичный ключ** (`id_ed25519.pub`) — открытая часть, её кладут на сервер. Аналогия: публичный ключ — замок, который вы раздаёте всем серверам; приватный — единственный ключ от всех этих замков. Сервер проверяет: «владеешь ли ты приватной половиной?» — и пускает без пароля. **Важно:** никогда не отправляйте приватный ключ по почте и мессенджерам и обязательно задавайте на него **парольную фразу** (passphrase) — тогда даже украденный файл бесполезен ворам. Раздавать можно только файл с `.pub`!',
        },
        {
          type: 'code',
          title: 'Создаём пару ключей и копируем публичную часть',
          code: `$ ssh-keygen -t ed25519 -C "student@kurs"
Generating public/private ed25519 key pair.
Enter file in which to save the key (/home/student/.ssh/id_ed25519):    # Enter
Enter passphrase (empty for no passphrase):     # задаём пароль на ключ
Your identification has been saved in /home/student/.ssh/id_ed25519
Your public key has been saved in /home/student/.ssh/id_ed25519.pub
$ ssh-copy-id student@127.0.0.1
Number of key(s) added: 1
$ ssh student@127.0.0.1
Enter passphrase for key '/home/student/.ssh/id_ed25519':   # пароль ключа, не системы
student@ubuntu:~$    # вошли!`,
        },
        {
          type: 'heading',
          text: 'Копируем файлы: scp и rsync',
        },
        {
          type: 'paragraph',
          text: 'Раз есть труба — по ней можно не только команды слать, но и файлы возить. **scp** (secure copy) — это привычный `cp`, только через SSH: адрес другой машины записывается как `пользователь@хост:путь`. А для больших папок правит **rsync** — умный синхронизатор: сравнивает файлы, передаёт только разницу и умеет продолжать после обрыва. Флаги `-avP`: **a**rchive (сохранить права и время), **v**erbose (рассказывать), **P**rogress (прогресс и докачивание).',
        },
        {
          type: 'code',
          title: 'Гоняем файлы через SSH: scp и rsync',
          code: `$ echo "секретный доклад" > doklad.txt
$ scp doklad.txt student@127.0.0.1:/tmp/
doklad.txt                    100%   17    12.4KB/s   00:00
$ scp student@127.0.0.1:/tmp/doklad.txt ./doklad-kopiya.txt
doklad.txt                    100%   17    15.1KB/s   00:00
$ cat doklad-kopiya.txt
секретный доклад
# а теперь rsync — синхронизируем целую папку:
$ mkdir -p proekt && touch proekt/{a.txt,b.txt,c.txt}
$ rsync -avP proekt/ student@127.0.0.1:/tmp/proekt-kopiya/
sending incremental file list
a.txt              0 100%    0.00kB/s
b.txt              0 100%    0.00kB/s
c.txt              0 100%    0.00kB/s
sent 219 bytes  received 76 bytes  total size 0
$ touch proekt/d.txt        # добавили один файл
$ rsync -avP proekt/ student@127.0.0.1:/tmp/proekt-kopiya/
sending incremental file list
d.txt              0 100%    0.00kB/s      # передался только новый!`,
        },
        {
          type: 'heading',
          text: 'Делаем SSH безопаснее',
        },
        {
          type: 'paragraph',
          text: 'Сервер SSH настраивается файлом `/etc/ssh/sshd_config`. Три настройки — классика гигиены: отключить вход по паролю (пускать только по ключам), запретить вход пользователю root и, опционально, сменить порт с 22 на нестандартный — чтобы боты-переборщики стучались в пустоту. После правки применяем настройки уже знакомым пультом: `sudo systemctl reload ssh` — это мягкий вариант `restart`: служба перечитывает конфигурацию, не разрывая текущие подключения.',
        },
        {
          type: 'code',
          title: 'Три строки безопасности в sshd_config',
          code: `$ sudo nano /etc/ssh/sshd_config
# находим и правим (или добавляем) строки:
PermitRootLogin no
PasswordAuthentication no
# Port 2222     # опционально: нестандартный порт
$ sudo systemctl reload ssh`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Железное правило: **не отключайте вход по паролю, пока не убедились, что вход по ключу работает**. Проверяйте в новом терминале, не закрывая старую сессию. Иначе получите сервер, в который никто (включая вас) зайти не может. На тренировочном localhost это не страшно, на боевом сервере — катастрофа.',
        },
        {
          type: 'paragraph',
          text: 'Проверьте себя: почему пара ключей безопаснее пароля, и что произойдёт, если кто-то украдёт файл `id_ed25519.pub`? А `id_ed25519` без парольной фразы? Администраторский минимум SSH: ключи ed25519, вход по паролю отключён, root по SSH не ходит, файлы копируются через scp/rsync. Модуль пройден — впереди модуль 8 «Безопасность и надёжность»: файрвол ufw, автообновления, резервные копии и журналы.',
        },
      ],
      tasks: [
        {
          title: 'Поднимите SSH-сервер и войдите сами к себе',
          difficulty: 1,
          description:
            'Установите `openssh-server`, убедитесь, что служба `ssh` работает, и подключитесь `ssh <ваш_пользователь>@127.0.0.1`. Внутри сеанса выполните `whoami` и `hostname`, затем выйдите командой `exit`.',
          hint: 'Проверка службы: `systemctl status ssh`. При первом подключении смело отвечайте `yes` на вопрос про отпечаток — это ваша же машина.',
          solution: `$ sudo apt install -y openssh-server
$ systemctl is-active ssh
active
$ ssh student@127.0.0.1
Are you sure you want to continue connecting (yes/no)? yes
student@127.0.0.1's password:
student@ubuntu:~$ whoami
student
student@ubuntu:~$ exit
logout
Connection to 127.0.0.1 closed.`,
        },
        {
          title: 'Создайте ключи и настройте вход без пароля',
          difficulty: 2,
          description:
            'Сгенерируйте ключевую пару ed25519 с парольной фразой, скопируйте публичный ключ себе же через `ssh-copy-id` и добейтесь, чтобы `ssh student@127.0.0.1` спрашивал парольную фразу ключа, а не пароль системы.',
          hint: 'Команды: `ssh-keygen -t ed25519`, затем `ssh-copy-id student@127.0.0.1`. Ключи лягут в `~/.ssh/` — посмотрите их через `ls -l ~/.ssh/`.',
          solution: `$ ssh-keygen -t ed25519 -C "student@kurs"
# Enter (путь по умолчанию), затем дважды вводим passphrase
$ ssh-copy-id student@127.0.0.1
Number of key(s) added: 1
$ ls -l ~/.ssh/
-rw------- 1 student student  464 Mar 10 12:00 id_ed25519      # приватный: только владельцу
-rw-r--r-- 1 student student  100 Mar 10 12:00 id_ed25519.pub  # публичный: можно раздавать
-rw------- 1 student student  800 Mar 10 12:05 authorized_keys # куда попал ключ
$ ssh student@127.0.0.1
Enter passphrase for key '/home/student/.ssh/id_ed25519':
student@ubuntu:~$    # вход по ключу работает!`,
        },
        {
          title: 'Отправьте папку через rsync',
          difficulty: 2,
          description:
            'Создайте папку `otchet/` с тремя текстовыми файлами и синхронизируйте её через rsync в `/tmp/otchet-backup/` на «удалённой» стороне (`student@127.0.0.1`). Затем измените один файл, повторите команду и убедитесь по выводу, что передался только он.',
          hint: 'Формат: `rsync -avP otchet/ student@127.0.0.1:/tmp/otchet-backup/`. Слеш в конце исходной папки важен: «содержимое папки», а не «сама папка».',
          solution: `$ mkdir otchet && cd otchet
$ echo "январь" > jan.txt; echo "февраль" > feb.txt; echo "март" > mar.txt
$ cd ..
$ rsync -avP otchet/ student@127.0.0.1:/tmp/otchet-backup/
sending incremental file list
jan.txt   feb.txt   mar.txt          # первый раз ушли все три
$ echo "март, исправлено" > otchet/mar.txt
$ rsync -avP otchet/ student@127.0.0.1:/tmp/otchet-backup/
sending incremental file list
mar.txt                             # а теперь — только изменившийся!`,
        },
        {
          title: 'Ужесточите SSH на тренировочном стенде',
          difficulty: 3,
          description:
            'На своей машине (где вы только что настроили вход по ключу на 127.0.0.1) отключите в `/etc/ssh/sshd_config` вход по паролю и вход для root, перечитайте конфигурацию и проверьте: подключение по ключу всё ещё работает. Подсказка для самоконтроля: `sudo sshd -t` проверит конфиг на ошибки до применения.',
          hint: 'Сначала убедитесь, что вход по ключу работает (задание 2)! Порядок: правка файла → `sudo sshd -t` → `sudo systemctl reload ssh` → проверка входа в НОВОМ терминале.',
          solution: `$ sudo nano /etc/ssh/sshd_config
# раскомментировать/изменить строки:
PermitRootLogin no
PasswordAuthentication no
$ sudo sshd -t          # проверка синтаксиса: молчит = ошибок нет
$ sudo systemctl reload ssh
$ ssh student@127.0.0.1
Enter passphrase for key '/home/student/.ssh/id_ed25519':
student@ubuntu:~$    # по ключу заходит — пароль теперь бесполезен
# если бы что-то пошло не так, вернуть всё просто:
# открыть файл снова, вернуть "PasswordAuthentication yes",
# sudo systemctl reload ssh`,
        },
      ],
      quiz: [
        {
          question: 'Что из себя представляет пара SSH-ключей?',
          options: [
            'Два пароля: один для входа, второй запасной',
            'Приватный ключ хранится только у вас, публичный кладётся на серверы; сервер проверяет пару математически, без передачи пароля',
            'Один ключ для шифрования, второй — для удаления логов',
            'Ключ root и ключ обычного пользователя',
          ],
          correctIndex: 1,
          explanation:
            'Приватный ключ (id_ed25519) никогда не покидает ваш компьютер. Публичный (id_ed25519.pub) можно раздавать серверам. Сервер проверяет владение приватной половиной, не видя её самой.',
        },
        {
          question: 'Зачем на приватный ключ ставить парольную фразу (passphrase)?',
          options: [
            'Чтобы ключ работал быстрее',
            'Чтобы сервер узнавал вас по голосу',
            'Чтобы даже украденный файл ключа был бесполезен без фразы',
            'Это требование формата ed25519, без фразы ключ не создаётся',
          ],
          correctIndex: 2,
          explanation:
            'Passphrase шифрует сам файл ключа. Если злоумышленник скопирует id_ed25519, без фразы он им не воспользуется. Фраза необязательна технически, но настоятельно рекомендуется.',
        },
        {
          question: 'Чем rsync выгодно отличается от scp при копировании больших папок?',
          options: [
            'rsync передаёт только изменившиеся файлы и умеет продолжать после обрыва',
            'rsync не требует SSH',
            'rsync работает только локально, зато очень быстро',
            'Ничем, scp всегда лучше',
          ],
          correctIndex: 0,
          explanation:
            'rsync сравнивает источник и приёмник и пересылает лишь разницу — на втором запуске папки из тысячи файлов уйдут только новые. scp каждый раз копирует всё целиком.',
        },
        {
          question: 'Почему нельзя отключать PasswordAuthentication, не проверив вход по ключу?',
          options: [
            'Потому что пароль тогда удалится навсегда из системы',
            'Потому что можно остаться без доступа к серверу: ключи не работают, а пароль уже не принимается',
            'Потому что SSH-сервер откажется запускаться',
            'Отключать можно в любой момент, система сама создаст ключи',
          ],
          correctIndex: 1,
          explanation:
            'Это классическая ошибка новичка: отключил пароль — а ключи не настроил — и сервер стал недоступен. Правило: сначала рабочий вход по ключу в новом терминале, и только потом отключение паролей.',
        },
      ],
    },
  ],
};
