import type { CourseModule } from '../types';

export const m03: CourseModule = {
  id: 'm03',
  number: 3,
  title: 'Терминал на полную мощность',
  rank: 'Пользователь',
  description:
    'Переходим от отдельных команд к настоящему владению терминалом: перенаправляем вывод, строим конвейеры, ищем текст и файлы, собираем статистику и учимся распаковывать архивы. После этого модуля чёрное окно терминала станет вашим любимым инструментом.',
  lessons: [
    {
      id: 'm03-l01',
      title: 'Потоки и перенаправление: >, >>, 2>, |',
      minutes: 25,
      intro:
        'Научитесь управлять результатом любой команды: сохранять вывод в файлы, отделять ошибки от данных и передавать вывод одной команды прямо на вход другой.',
      blocks: [
        {
          type: 'paragraph',
          text: 'В модуле 2 вы уже умеете главное: перемещаться по папкам (`cd`, `pwd`), смотреть содержимое (`ls`), создавать файлы и папки (`touch`, `mkdir`) и читать их (`cat`, `less`). Но до сих пор результат каждой команды просто показывался на экране — и исчезал. Сегодня мы научимся им **управлять**.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Представьте кассу в магазине. Вы говорите заказ — это **вход**. Кассир выдаёт чек — это **выход**. А если товара нет, кассир сообщает об ошибке **отдельно**, не записывая её в чек. У каждой команды в Linux есть ровно такие же три «окошка».',
        },
        { type: 'heading', text: 'Три стандартных потока' },
        {
          type: 'paragraph',
          text: 'Каждая программа в Linux при запуске получает три **потока** (stream): **stdin** (стандартный ввод, номер 0) — откуда программа читает данные, обычно клавиатура; **stdout** (стандартный вывод, номер 1) — куда пишет результат, обычно экран; **stderr** (стандартный вывод ошибок, номер 2) — куда пишет сообщения об ошибках, тоже экран. Раз у всех трёх есть номера — их можно перенаправлять.',
        },
        {
          type: 'table',
          headers: ['Поток', 'Номер', 'Куда по умолчанию', 'Оператор'],
          rows: [
            ['stdin — ввод', '0', 'Клавиатура', '< (используется редко)'],
            ['stdout — результат', '1', 'Экран', '> или >>'],
            ['stderr — ошибки', '2', 'Экран', '2>'],
          ],
        },
        {
          type: 'code',
          title: 'Перенаправляем вывод в файл',
          code: `$ echo "Привет, мир!"
Привет, мир!
$ echo "Привет, мир!" > hello.txt   # вывод ушёл не на экран, а в файл
$ cat hello.txt
Привет, мир!`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Одиночный `>` **полностью перезаписывает файл**! Если `hello.txt` уже существовал, старое содержимое исчезнет мгновенно, без корзины и без вопроса «вы уверены?». Хотите дописать в конец — используйте `>>`.',
        },
        {
          type: 'code',
          title: '>> добавляет в конец файла',
          code: `$ echo "первая строка" > notes.txt
$ echo "вторая строка" >> notes.txt    # добавили, а не стёрли
$ echo "третья строка" >> notes.txt
$ cat notes.txt
первая строка
вторая строка
третья строка`,
        },
        { type: 'heading', text: 'Ловим ошибки: 2>' },
        {
          type: 'paragraph',
          text: 'Зачем ошибкам отдельный поток? Чтобы они не смешивались с результатом. Когда вы сохраняете список файлов в отчёт, сообщение «Permission denied» не должно попасть внутрь отчёта — оно должно уйти отдельно. Для этого служит оператор `2>`, где 2 — номер потока ошибок. А путь `/dev/null` — это «чёрная дыра» Linux: всё, что туда направлено, просто исчезает.',
        },
        {
          type: 'code',
          title: 'Ошибки — в файл или в никуда',
          code: `$ ls /несуществующая-папка
ls: cannot access '/несуществующая-папка': No such file or directory
$ ls /несуществующая-папка 2> errors.txt   # ошибка ушла в файл, экран чист
$ cat errors.txt
ls: cannot access '/несуществующая-папка': No such file or directory
$ ls /несуществующая-папка 2> /dev/null    # просто выбросить ошибку`,
        },
        { type: 'heading', text: 'Конвейер: |' },
        {
          type: 'paragraph',
          text: 'Самая знаменитая идея Unix — **конвейер** (pipe). Символ `|` берёт вывод команды слева и подаёт его на вход команде справа — как лента на заводе передаёт деталь от станка к станку. Маленькие простые команды, соединённые конвейером, решают большие задачи.',
        },
        {
          type: 'code',
          title: 'Команды работают в связке',
          code: `$ ls /etc | wc -l        # посчитать, сколько файлов и папок в /etc
209
$ cat /etc/passwd | sort | head -5   # первые 5 строк отсортированного списка
_apt:x:105:65534::/nonexistent:/usr/sbin/nologin
backup:x:34:34:backup:/var/backups:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
games:x:5:60:games:/usr/games:/usr/sbin/nologin`,
        },
        {
          type: 'list',
          items: [
            '`команда > файл` — записать результат в файл (с перезаписью)',
            '`команда >> файл` — дописать результат в конец файла',
            '`команда 2> файл` — записать туда ошибки',
            '`команда > файл 2>&1` — записать и результат, и ошибки в один файл',
            '`команда1 | команда2 | команда3` — цепочка обработки',
          ],
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Четыре заклинания этого урока: `>` — в файл (перезапись!), `>>` — в конец файла, `2>` — ошибки в файл, `|` — вывод одной команды становится входом другой.',
        },
        { type: 'heading', text: 'Собираем всё вместе' },
        {
          type: 'paragraph',
          text: 'Потоки и конвейеры — фундамент всего остального в терминале. В следующем уроке мы добавим в цепочки `grep` — команду поиска текста, и вы сможете находить нужную строку в файле любого размера за долю секунды.',
        },
      ],
      tasks: [
        {
          title: 'Сохраните вывод в файл',
          difficulty: 1,
          description:
            'С помощью `echo` и перенаправления создайте файл `hello.txt`, содержащий строку «Учу потоки!». Затем командой `cat` убедитесь, что файл создан и содержит именно эту строку.',
          hint: 'Команда `echo` печатает текст, а `>` отправляет этот текст в файл вместо экрана.',
          solution: `$ echo "Учу потоки!" > hello.txt   # экран остался пустым — текст ушёл в файл
$ cat hello.txt
Учу потоки!`,
        },
        {
          title: 'Соберите список дел по строкам',
          difficulty: 2,
          description:
            'Создайте файл `todo.txt` с тремя строками: «купить молоко», «позвонить маме», «выучить pipe». Каждую строку добавляйте отдельной командой, не стирая предыдущие. В конце выведите файл целиком.',
          hint: 'Первую строку можно записать через `>`, а следующие — только через `>>`, иначе сотрёте уже написанное.',
          solution: `$ echo "купить молоко" > todo.txt
$ echo "позвонить маме" >> todo.txt    # >> дописывает в конец
$ echo "выучить pipe" >> todo.txt
$ cat todo.txt
купить молоко
позвонить маме
выучить pipe`,
        },
        {
          title: 'Спрячьте ошибки под ковёр',
          difficulty: 2,
          description:
            'Выполните `ls /root` от своего пользователя — система ответит ошибкой «Permission denied». Теперь выполните ту же команду так, чтобы ошибка попала в файл `ls-errors.txt`, а затем — так, чтобы ошибка вообще исчезла. Убедитесь, что файл с ошибкой создан.',
          hint: 'Ошибки живут в потоке номер 2. «Чёрная дыра» системы называется `/dev/null`.',
          solution: `$ ls /root
ls: cannot open directory '/root': Permission denied
$ ls /root 2> ls-errors.txt      # ошибка ушла в файл
$ cat ls-errors.txt
ls: cannot open directory '/root': Permission denied
$ ls /root 2> /dev/null          # ошибка выброшена в никуда`,
        },
        {
          title: 'Постройте свой первый конвейер',
          difficulty: 3,
          description:
            'Одной командой с конвейером посчитайте, сколько пользователей записано в файле `/etc/passwd` (каждая строка — один пользователь), и сохраните это число в файл `users-count.txt`. Проверьте содержимое файла.',
          hint: 'Посчитать строки умеет `wc -l`. Вывод этой связки перенаправьте через `>` в файл.',
          solution: `$ cat /etc/passwd | wc -l > users-count.txt   # посчитали и сохранили
$ cat users-count.txt
31
# то же самое можно короче, без cat:
$ wc -l < /etc/passwd > users-count.txt`,
        },
      ],
      quiz: [
        {
          question: 'Что делает команда `echo "текст" > file.txt`, если file.txt уже существует?',
          options: [
            'Полностью перезаписывает файл новым содержимым',
            'Дописывает текст в конец файла',
            'Отказывается работать и выдаёт ошибку',
            'Переименовывает старый файл в file.txt.bak',
          ],
          correctIndex: 0,
          explanation:
            'Одиночный `>` всегда перезаписывает файл без предупреждения. Чтобы дописать в конец, нужен двойной оператор `>>`.',
        },
        {
          question: 'Куда команда пишет сообщения об ошибках?',
          options: [
            'В стандартный вывод (stdout, поток 1)',
            'В файл errors.log автоматически',
            'В стандартный вывод ошибок (stderr, поток 2)',
            'В системный журнал /var/log/errors',
          ],
          correctIndex: 2,
          explanation:
            'Ошибки идут в отдельный поток stderr (номер 2), чтобы их можно было перенаправить (`2>`) независимо от нормального результата.',
        },
        {
          question: 'Что делает символ `|` в команде `ls /etc | wc -l`?',
          options: [
            'Разделяет две независимые команды, которые выполняются по очереди',
            'Передаёт вывод `ls /etc` на вход команде `wc -l`',
            'Сравнивает результаты двух команд',
            'Запускает `wc -l` только если `ls` завершилась с ошибкой',
          ],
          correctIndex: 1,
          explanation:
            'Это конвейер (pipe): стандартный вывод команды слева становится стандартным вводом команды справа. Здесь список файлов из /etc попадает в счётчик строк.',
        },
        {
          question: 'Что произойдёт при выполнении `ls /root 2> /dev/null` от обычного пользователя?',
          options: [
            'Список файлов /root появится на экране',
            'Сообщение об ошибке будет скрыто, а на экране не появится ничего',
            'Файл /dev/null будет удалён',
            'Команда запросит пароль администратора',
          ],
          correctIndex: 1,
          explanation:
            'Ошибка «Permission denied» уйдёт в /dev/null — «чёрную дыру», где данные исчезают. Нормального вывода у команды нет, поэтому экран останется чистым.',
        },
      ],
    },
    {
      id: 'm03-l02',
      title: 'Поиск текста: grep и основы регулярных выражений',
      minutes: 25,
      intro:
        'Научитесь находить нужные строки в файлах любого размера за долю секунды — с помощью grep и простых шаблонов регулярных выражений.',
      blocks: [
        {
          type: 'paragraph',
          text: 'В прошлом уроке вы научились сохранять вывод и строить конвейеры. Теперь добавим в арсенал самую востребованную команду-поисковик. Представьте: в файле `/etc/passwd` тридцать строк, в логе сервера — тридцать тысяч, а вам нужна одна. Читать глазами? Нет, для этого есть **grep**.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: '`grep` — это Ctrl+F из текстового редактора, но для терминала и на стероидах. Название расшифровывается как «global regular expression print»: «найди по шаблону везде и напечатай».',
        },
        { type: 'heading', text: 'Первый поиск: grep' },
        {
          type: 'paragraph',
          text: 'Синтаксис простой: `grep шаблон файл`. Команда читает файл и печатает **только те строки**, где встретился шаблон. Можно искать и в выводе другой команды через конвейер — этот приём вы будете использовать каждый день.',
        },
        {
          type: 'code',
          title: 'Ищем строки со словом',
          code: `$ grep student /etc/passwd      # строки, где есть слово student
student:x:1000:1000:Student,,,:/home/student:/bin/bash
$ ls /etc | grep conf             # только имена файлов с "conf"
adduser.conf
debconf.conf
deluser.conf
ld.so.conf
nsswitch.conf
resolv.conf
rsyslog.conf
sysctl.conf
ucf.conf`,
        },
        {
          type: 'code',
          title: 'Полезные флаги grep',
          code: `$ grep -n root /etc/passwd       # -n: показать номера строк
1:root:x:0:0:root:/root:/bin/bash
$ grep -c nologin /etc/passwd     # -c: не печатать строки, а посчитать их
19
$ grep -v nologin /etc/passwd     # -v: наоборот, строки БЕЗ слова
root:x:0:0:root:/root:/bin/bash
student:x:1000:1000:Student,,,:/home/student:/bin/bash
$ grep -i ERROR /var/log/dpkg.log | head -2   # -i: без учёта регистра`,
        },
        {
          type: 'list',
          items: [
            '`-i` (ignore case) — искать без учёта регистра: Error, ERROR и error — одно и то же',
            '`-n` (number) — показывать номер строки',
            '`-v` (invert) — инвертировать: показать строки, где шаблона НЕТ',
            '`-c` (count) — только количество совпадений',
            '`-r` (recursive) — искать во всех файлах папки и подпапок',
          ],
        },
        { type: 'heading', text: 'Регулярные выражения: язык шаблонов' },
        {
          type: 'paragraph',
          text: 'Пока мы искали точное слово. Но grep понимает **регулярные выражения** (regular expressions, сокращённо regex) — мини-язык для описания шаблонов текста. Например: «строка, которая начинается со слова student» или «любое слово из 5 букв, начинающееся на s». Пять символов покрывают 90% задач.',
        },
        {
          type: 'table',
          headers: ['Символ', 'Значение', 'Пример'],
          rows: [
            ['^', 'начало строки', "'^root' — строки, начинающиеся с root"],
            ['$', 'конец строки', "'bash$' — строки, заканчивающиеся на bash"],
            ['.', 'любой один символ', "'s.dent' — student, sedent..."],
            ['*', 'предыдущий символ 0+ раз', "'ab*c' — ac, abc, abbc..."],
            ['[abc]', 'один символ из набора', "'[0-9]' — любая цифра"],
          ],
        },
        {
          type: 'code',
          title: 'Шаблоны в деле',
          code: `$ grep '^student' /etc/passwd    # начинается со student
student:x:1000:1000:Student,,,:/home/student:/bin/bash
$ grep 'bash$' /etc/passwd        # заканчивается на bash
root:x:0:0:root:/root:/bin/bash
student:x:1000:1000:Student,,,:/home/student:/bin/bash
$ grep '^#' /etc/ssh/sshd_config | head -3    # строки-комментарии
#       $OpenBSD: sshd_config,v 1.103 2018/04/09 20:41:22 tj Exp $
# This is the sshd server system-wide configuration file.  See
# sshd_config(5) for more information.`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Шаблон всегда берите в **одинарные кавычки**: `grep \'bash$\' файл`. Иначе bash попытается сам истолковать символы `^`, `$`, `*` ещё до того, как их увидит grep, — и результат будет непредсказуемым.',
        },
        { type: 'heading', text: 'grep в связке с другими командами' },
        {
          type: 'paragraph',
          text: 'Чаще всего grep стоит в середине или в конце конвейера: одна команда добывает данные, grep отфильтровывает нужное, следующая считает или сохраняет результат. Это и есть «терминал на полную мощность».',
        },
        {
          type: 'code',
          title: 'Типичные связки',
          code: `$ ls /etc | grep -c conf         # сколько .conf-файлов в /etc?
22
$ grep '^root' /etc/passwd | wc -l  # сколько строк начинается с root
1
$ grep -r 'student' /etc/ 2>/dev/null | head -3   # искать во всём /etc
/etc/subuid:student:100000:65536
/etc/subgid:student:100000:65536
/etc/passwd:student:x:1000:1000:Student,,,:/home/student:/bin/bash`,
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя вслух: чем отличаются флаги `-v` и `-c`? Что означают `^` и `$` в шаблоне? Зачем шаблон брать в кавычки? Ответили — двигаемся дальше.',
        },
        { type: 'heading', text: 'Закрепим' },
        {
          type: 'paragraph',
          text: 'Теперь вы умеете искать иголку в стоге сена любого размера. В следующем уроке добавим остальные «рабочие лошадки»: `find` ищет сами файлы, а `sort`, `uniq`, `wc` и `cut` превращают сырые данные в аккуратные отчёты.',
        },
      ],
      tasks: [
        {
          title: 'Найдите себя в системе',
          difficulty: 1,
          description:
            'Найдите в файле `/etc/passwd` строку, описывающую вашего пользователя (в курсе это `student`). Выведите её на экран, а затем сохраните в файл `me.txt`.',
          hint: 'Один шаблон, один файл: `grep шаблон файл`. Сохранение — через `>` из первого урока.',
          solution: `$ grep student /etc/passwd
student:x:1000:1000:Student,,,:/home/student:/bin/bash
$ grep student /etc/passwd > me.txt
$ cat me.txt
student:x:1000:1000:Student,,,:/home/student:/bin/bash`,
        },
        {
          title: 'Посчитайте «невходящих» пользователей',
          difficulty: 2,
          description:
            'В `/etc/passwd` у большинства системных пользователей оболочка `/usr/sbin/nologin` — войти в систему под ними нельзя. Посчитайте, сколько строк файла содержат слово `nologin`, одной командой.',
          hint: 'Нужен флаг, который вместо печати строк выводит их количество.',
          solution: `$ grep -c nologin /etc/passwd
19
# проверка связкой из прошлого урока:
$ grep nologin /etc/passwd | wc -l
19`,
        },
        {
          title: 'Отфильтруйте список файлов',
          difficulty: 2,
          description:
            'Выведите список файлов и папок в `/etc`, имена которых **начинаются** на букву `h`. Должны получиться, например, `hostname`, `hosts`, `host.conf`.',
          hint: '«Начинается на» — это символ начала строки в регулярном выражении. Список имён даст `ls /etc`, а отфильтрует его grep в конвейере.',
          solution: `$ ls /etc | grep '^h'
host.conf
hostname
hosts
hosts.allow
hosts.deny`,
        },
        {
          title: 'Найдите веб-сервисы в /etc/services',
          difficulty: 3,
          description:
            'Файл `/etc/services` содержит список сетевых служб и их портов. Найдите в нём все строки, где встречается слово `http` (в любом регистре), выведите первые 5, а затем отдельной командой посчитайте общее количество таких строк.',
          hint: 'Понадобятся флаг «без учёта регистра», команда `head` и связка `grep ... | wc -l`.',
          solution: `$ grep -i http /etc/services | head -5
http            80/tcp          www             # WorldWideWeb HTTP
https           443/tcp                         # http protocol over TLS/SSL
http-alt        8080/tcp        webcache        # WWW caching service
http-alt        8080/udp
https           443/udp
$ grep -ic http /etc/services
31`,
        },
      ],
      quiz: [
        {
          question: 'Что делает команда `grep -i error log.txt`?',
          options: [
            'Ищет строки со словом error только в начале строки',
            'Ищет строки со словом error без учёта регистра: error, Error, ERROR',
            'Удаляет строки со словом error из файла',
            'Показывает только количество строк с error',
          ],
          correctIndex: 1,
          explanation:
            'Флаг `-i` (ignore case) отключает чувствительность к регистру, поэтому находятся все варианты написания слова.',
        },
        {
          question: 'Что означает символ `^` в шаблоне `\'^student\'`?',
          options: [
            'Любой один символ',
            'Конец строки',
            'Возведение в степень',
            'Начало строки',
          ],
          correctIndex: 3,
          explanation:
            '`^` «привязывает» шаблон к началу строки: совпадут только строки, которые начинаются со student. Конец строки обозначается символом `$`.',
        },
        {
          question: 'Какая команда выведет строки файла `data.txt`, в которых НЕТ слова test?',
          options: [
            'grep -v test data.txt',
            'grep -c test data.txt',
            'grep -i test data.txt',
            'grep --no test data.txt',
          ],
          correctIndex: 0,
          explanation:
            'Флаг `-v` (invert) инвертирует поиск: печатаются строки, где шаблон не найден. Такого флага, как `--no`, у grep не существует.',
        },
        {
          question: 'Зачем шаблон регулярного выражения берут в одинарные кавычки?',
          options: [
            'Так команда работает быстрее',
            'Без кавычек grep вообще не запускается',
            'Чтобы bash не обработал спецсимволы ($, *, ^) раньше, чем их увидит grep',
            'Чтобы в шаблоне можно было использовать русские буквы',
          ],
          correctIndex: 2,
          explanation:
            'Символы `$`, `*`, `^` — специальные и для оболочки bash тоже. Одинарные кавычки «прячут» их от bash и передают grep в первозданном виде.',
        },
      ],
    },
    {
      id: 'm03-l03',
      title: 'Находим и обрабатываем: find, sort, uniq, wc, cut',
      minutes: 30,
      intro:
        'Научитесь искать файлы по имени и свойствам, а результаты — сортировать, чистить от дубликатов, считать и резать на колонки. Пять команд, которые вместе заменяют целый табличный редактор.',
      blocks: [
        {
          type: 'paragraph',
          text: 'Вы уже умеете искать **внутри** файлов (`grep`) и соединять команды конвейером. Осталось два вопроса: как найти **сами файлы**, когда вы помните только кусочек имени, и как превратить найденное в аккуратный отчёт. На них отвечают пять команд этого урока.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: '`find` — это сыщик, которому вы даёте приметы («имя начинается на report», «папка», «изменён вчера»), а он обходит все комнаты дома и выкладывает список находок. Работает медленнее обычного `ls`, зато не пропускает ни одной подпапки.',
        },
        { type: 'heading', text: 'find: найди мне файл' },
        {
          type: 'paragraph',
          text: 'Формат: `find где_искать -критерий значение`. Самые нужные критерии: `-name "шаблон"` — по имени (звёздочка `*` означает «любые символы»), `-type f` — только файлы, `-type d` — только папки, `-size +10M` — больше 10 мегабайт. `find` заглядывает во все вложенные папки автоматически.',
        },
        {
          type: 'code',
          title: 'Три типичных поиска',
          code: `$ find ~ -name "notes.txt"        # искать в домашней папке по имени
/home/student/notes.txt
$ find /etc -name "*.conf" | head -5   # первые пять .conf-файлов в /etc
/etc/ucf.conf
/etc/sysctl.conf
/etc/rsyslog.conf
/etc/resolv.conf
/etc/nsswitch.conf
$ find ~ -type d -name "projects"      # искать только ПАПКИ (-type d)
/home/student/projects`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Ища по всей системе (`find / ...`), вы получите десятки строк «Permission denied» — обычному пользователю закрыты системные папки. Это нормально. Прячьте ошибки знакомым приёмом: `find / -name "*.log" 2>/dev/null`.',
        },
        { type: 'heading', text: 'sort и uniq: порядок и чистота' },
        {
          type: 'paragraph',
          text: '`sort` сортирует строки по алфавиту (с флагом `-n` — по числам), а `uniq` убирает повторы. Важный нюанс: `uniq` видит только **соседние** одинаковые строки, поэтому их почти всегда запускают парой: сначала `sort`, потом `uniq`. А флаг `-c` у uniq не просто убирает дубликаты, а считает их.',
        },
        {
          type: 'code',
          title: 'Мини-отчёт из хаоса',
          code: `$ cat fruits.txt
банан
яблоко
банан
апельсин
яблоко
банан
$ sort fruits.txt | uniq          # сортировка + убрать повторы
апельсин
банан
яблоко
$ sort fruits.txt | uniq -c       # то же, но с подсчётом каждого
      1 апельсин
      3 банан
      2 яблоко`,
        },
        { type: 'heading', text: 'wc: считаем всё' },
        {
          type: 'code',
          title: 'wc (word count) считает строки, слова и байты',
          code: `$ wc fruits.txt                 # строки, слова, байты
6 6 76 fruits.txt
$ wc -l /etc/passwd               # только строки: сколько пользователей
31 /etc/passwd
$ ls /etc | wc -l                 # сколько файлов и папок в /etc
209`,
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Почему 76, если букв в файле меньше? Третья колонка `wc` — это **байты**, а не символы. В кодировке UTF-8 каждая русская буква занимает 2 байта (плюс по байту на перевод строки). Хотите посчитать именно символы — есть флаг `wc -m`: он покажет 41.',
        },
        { type: 'heading', text: 'cut: режем текст на колонки' },
        {
          type: 'paragraph',
          text: 'Файлы вроде `/etc/passwd` — это таблицы, где колонки разделены двоеточием. `cut` вырезает нужные колонки: флаг `-d` задаёт **разделитель** (delimiter), флаг `-f` — номера **полей** (fields), которые оставить. Колонки нумеруются с единицы.',
        },
        {
          type: 'code',
          title: 'Вырезаем нужные поля',
          code: `$ cut -d: -f1 /etc/passwd | head -4      # только первое поле — имена
root
daemon
bin
sys
$ cut -d: -f1,7 /etc/passwd | head -3   # поля 1 и 7: имя и оболочка
root:/bin/bash
daemon:/usr/sbin/nologin
bin:/usr/sbin/nologin`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Философия Unix: каждая программа делает одно дело, но делает его отлично, а конвейер `|` собирает их в команду. `find` нашёл, `grep` отфильтровал, `cut` вырезал, `sort` упорядочил, `uniq` почистил, `wc` посчитал.',
        },
        { type: 'heading', text: 'Боевой пример: статистика оболочек' },
        {
          type: 'code',
          title: 'Одна строка — целый отчёт',
          code: `$ cut -d: -f7 /etc/passwd | sort | uniq -c | sort -rn
     19 /usr/sbin/nologin
      2 /bin/bash
      1 /bin/sync
# читаем слева направо: вырезать колонку 7 (оболочка),
# отсортировать, посчитать повторы, отсортировать по числу убывания`,
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя: почему `uniq` почти всегда идёт после `sort`? Что означают `-d:` и `-f1` у `cut`? Как `find` отличить файлы от папок? Совет: читайте длинные конвейеры вслух слева направо, как рецепт, — и скоро будете составлять их сами.',
        },
      ],
      tasks: [
        {
          title: 'Посчитайте пользователей системы',
          difficulty: 1,
          description:
            'Одной командой выведите, сколько строк (то есть учётных записей) содержится в файле `/etc/passwd`.',
          hint: 'Нужен `wc` с флагом «только строки».',
          solution: `$ wc -l /etc/passwd
31 /etc/passwd`,
        },
        {
          title: 'Найдите все файлы на букву h в /etc',
          difficulty: 2,
          description:
            'Командой `find` найдите в папке `/etc` все файлы, чьи имена начинаются на букву `h` (шаблон `h*`). Среди находок должны быть `hostname`, `hosts`, `host.conf`.',
          hint: 'Шаблон имени передаётся критерием `-name`, а звёздочку возьмите в кавычки, чтобы её не трогал bash.',
          solution: `$ find /etc -maxdepth 1 -name "h*"
/etc/host.conf
/etc/hostname
/etc/hosts
/etc/hosts.allow
/etc/hosts.deny`,
        },
        {
          title: 'Почистите список от дубликатов',
          difficulty: 2,
          description:
            'Создайте файл `tags.txt` со строками: linux, bash, linux, ubuntu, bash, linux (каждая строка — отдельной командой `echo ... >>`). Затем выведите список уникальных значений с количеством повторов каждого, отсортировав по убыванию частоты.',
          hint: 'Сначала `sort`, потом `uniq -c`, и в конце ещё один `sort` с числовой сортировкой в обратном порядке.',
          solution: `$ echo linux > tags.txt
$ echo bash >> tags.txt
$ echo linux >> tags.txt
$ echo ubuntu >> tags.txt
$ echo bash >> tags.txt
$ echo linux >> tags.txt
$ sort tags.txt | uniq -c | sort -rn
      3 linux
      2 bash
      1 ubuntu`,
        },
        {
          title: 'Составьте алфавитный список пользователей',
          difficulty: 3,
          description:
            'Постройте конвейер, который вырежет из `/etc/passwd` только имена пользователей (первое поле, разделитель — двоеточие), отсортирует их по алфавиту и сохранит в файл `users.txt`. Выведите первые 5 строк результата.',
          hint: 'Три команды в цепочке: cut → sort → перенаправление в файл. Потом `head` для проверки.',
          solution: `$ cut -d: -f1 /etc/passwd | sort > users.txt
$ head -5 users.txt
_apt
backup
bin
daemon
games`,
        },
      ],
      quiz: [
        {
          question: 'Почему `uniq` почти всегда запускают после `sort`?',
          options: [
            'uniq работает быстрее с отсортированными данными',
            'uniq убирает только СОСЕДНИЕ одинаковые строки, а sort собирает дубликаты рядом',
            'sort требует, чтобы после него шла другая команда',
            'Это просто традиция, технической причины нет',
          ],
          correctIndex: 1,
          explanation:
            'uniq сравнивает строку только с предыдущей. Без предварительной сортировки повторы, разбросанные по файлу, останутся на месте.',
        },
        {
          question: 'Что выведет команда `cut -d: -f1 /etc/passwd`?',
          options: [
            'Первую строку файла',
            'Список имён пользователей — первое поле каждой строки до двоеточия',
            'Все строки, содержащие двоеточие',
            'Файл, разрезанный на части по одному символу',
          ],
          correctIndex: 1,
          explanation:
            '`-d:` задаёт разделитель «двоеточие», `-f1` — вывести первое поле. В /etc/passwd первое поле каждой строки — имя пользователя.',
        },
        {
          question: 'Как найти в домашней папке все файлы с расширением .txt?',
          options: [
            'find ~ -name "*.txt"',
            'find ~ -type txt',
            'grep ~ "*.txt"',
            'ls ~ | find .txt',
          ],
          correctIndex: 0,
          explanation:
            'У find поиск по имени — это критерий `-name` с шаблоном в кавычках. Звёздочка `*` означает «любые символы».',
        },
        {
          question: 'Что делает флаг `-c` у команды `uniq`?',
          options: [
            'Сравнивает строки без учёта регистра',
            'Показывает только строки-дубликаты',
            'Перед каждой уникальной строкой печатает, сколько раз она встретилась',
            'Выводит общее количество строк в файле',
          ],
          correctIndex: 2,
          explanation:
            '`-c` (count) добавляет счётчик повторов. Классическая связка `sort | uniq -c | sort -rn` строит «топ» самых частых значений.',
        },
      ],
    },
    {
      id: 'm03-l04',
      title: 'Архивы и справка: tar, gzip, man, --help, history',
      minutes: 25,
      intro:
        'Научитесь упаковывать целые папки в один компактный архив и — что важнее — получать помощь прямо из терминала: встроенная справка man и история команд history сделают вас самостоятельным.',
      blocks: [
        {
          type: 'paragraph',
          text: 'Это финальный урок модуля. Вы уже перенаправляете потоки, строите конвейеры и ищете что угодно. Осталось два практических навыка: упаковать результаты работы в **архив** (например, чтобы отправить коллеге или сохранить про запас) и научиться **самостоятельно находить ответы**, не гугля каждый флаг.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Архив — это чемодан для файлов. `tar` собирает много файлов и папок в один «чемодан» (файл `.tar`), а `gzip` этот чемодан **сжимает**, как вакуумный пакет (получается `.tar.gz`). В Ubuntu они почти всегда работают вместе.',
        },
        { type: 'heading', text: 'tar + gzip: упаковываем папку' },
        {
          type: 'paragraph',
          text: 'Команда выглядит устрашающе, но её буквы легко расшифровать: `c` — create (создать), `z` — сжать через gzip, `v` — verbose (показывать, что происходит), `f` — file (дальше идёт имя файла архива). Для распаковки `c` меняется на `x` — extract (извлечь).',
        },
        {
          type: 'code',
          title: 'Создаём и проверяем архив',
          code: `$ tar -czvf backup.tar.gz projects/    # упаковать папку projects
projects/
projects/report.txt
projects/todo.txt
$ ls -lh backup.tar.gz
-rw-rw-r-- 1 student student 220 июн 10 12:31 backup.tar.gz`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Буква `f` всегда должна стоять **последней** в группе флагов, потому что сразу после неё идёт имя файла: `tar -czvf архив.tar.gz папка/`. Поставите `f` в середину — tar попытается использовать следующую букву как имя файла.',
        },
        {
          type: 'code',
          title: 'Смотрим внутрь и распаковываем',
          code: `$ tar -tzvf backup.tar.gz     # t (list) — посмотреть, не распаковывая
drwxr-xr-x student/student   0 2025-06-10 12:30 projects/
-rw-rw-r-- student/student  35 2025-06-10 12:30 projects/report.txt
-rw-rw-r-- student/student  12 2025-06-10 12:30 projects/todo.txt
$ mkdir restore
$ tar -xzvf backup.tar.gz -C restore/   # x (extract) — распаковать в restore
projects/
projects/report.txt
projects/todo.txt
$ ls restore/
projects`,
        },
        { type: 'heading', text: 'Справка всегда под рукой: man и --help' },
        {
          type: 'paragraph',
          text: 'Запомнить все флаги всех команд невозможно — и не нужно. В Ubuntu встроена полная документация: `man команда` (от manual — «руководство») открывает подробную страницу справки, а `команда --help` выводит короткую шпаргалку прямо в терминал. Опытные администраторы заглядывают в man постоянно — это признак профессионализма, а не слабости.',
        },
        {
          type: 'code',
          title: 'Два способа спросить у системы',
          code: `$ man ls
# откроется интерактивная страница: листайте стрелками или пробелом,
# ищите текст клавишей /, выход — клавиша q
$ ls --help | head -6
Usage: ls [OPTION]... [FILE]...
List information about the FILEs (the current directory by default).
Sort entries alphabetically if none of -cftuvSUX nor --sort is specified.

Mandatory arguments to long options are mandatory for short options too.
  -a, --all                  do not ignore entries starting with .`,
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Не учите флаги наизусть — учитесь **находить** их. Запутались в `tar`? `man tar` или `tar --help`. Забыли флаг у `sort`? `sort --help`. Система сама себя документирует.',
        },
        { type: 'heading', text: 'history: что я делал вчера?' },
        {
          type: 'paragraph',
          text: 'Bash запоминает всё, что вы вводили. Команда `history` показывает пронумерованный список прошлых команд. Найти нужную помогает связка с `grep`, а волшебная комбинация **Ctrl+R** включает интерактивный поиск: нажимаете и начинаете печатать кусок команды — bash сам подтягивает подходящие строки из истории.',
        },
        {
          type: 'code',
          title: 'История работает на вас',
          code: `$ history | tail -5          # последние 5 команд
  498  mkdir restore
  499  tar -xzvf backup.tar.gz -C restore/
  500  ls restore/
  501  man ls
  502  history | tail -5
$ history | grep tar           # все команды, где был tar
  480  tar -czvf backup.tar.gz projects/
  488  tar -tzvf backup.tar.gz
  499  tar -xzvf backup.tar.gz -C restore/`,
        },
        {
          type: 'list',
          items: [
            '**Стрелка вверх** — листать предыдущие команды по одной',
            '`!!` — повторить самую последнюю команду целиком',
            '`!499` — выполнить команду под номером 499 из истории',
            '**Ctrl+R** — интерактивный поиск по истории (ESC — отмена)',
            '`history | grep слово` — найти все команды с этим словом',
          ],
        },
        {
          type: 'callout',
          variant: 'check',
          text: 'Проверьте себя: чем отличаются `tar -czvf` и `tar -xzvf`? Как выйти из man? Как найти в истории все команды с `grep`?',
        },
        { type: 'heading', text: 'Что дальше' },
        {
          type: 'paragraph',
          text: 'Потоки, конвейеры, поиск, обработка текста, архивы и справка — внушительный арсенал! Но есть ещё одна тема, без которой картина терминала неполная. Замечали, что команда `history` помнит прошлое, а вот ваши собственные настройки после перезахода исчезают? В последнем уроке модуля разберёмся с **переменными окружения** и файлом `~/.bashrc` — и вы наконец настроите терминал под себя так, чтобы настройки не пропадали никогда.',
        },
      ],
      tasks: [
        {
          title: 'Упакуйте свой первый архив',
          difficulty: 1,
          description:
            'Создайте папку `mydocs` с двумя файлами внутри (`a.txt`, `b.txt` — содержимое любое, хоть одна строка через `echo`). Затем упакуйте папку в архив `mydocs.tar.gz` и убедитесь, что архив появился (`ls -lh`).',
          hint: 'Вспомните четыре буквы: create, zip(gzip), verbose, file. Имя архива идёт сразу после флагов, папка — в конце.',
          solution: `$ mkdir mydocs
$ echo "первый документ" > mydocs/a.txt
$ echo "второй документ" > mydocs/b.txt
$ tar -czvf mydocs.tar.gz mydocs/
mydocs/
mydocs/a.txt
mydocs/b.txt
$ ls -lh mydocs.tar.gz
-rw-rw-r-- 1 student student 178 июн 10 13:05 mydocs.tar.gz`,
        },
        {
          title: 'Распакуйте архив «на другом компьютере»',
          difficulty: 2,
          description:
            'Сымитируйте перенос архива: распакуйте `mydocs.tar.gz` из прошлого задания в новую папку `restored` (её нужно создать). Перед распаковкой посмотрите содержимое архива, ничего не извлекая. После — проверьте, что файлы на месте.',
          hint: 'Для просмотра без распаковки вместо `x` используется `t`. Папку назначения задаёт флаг `-C`.',
          solution: `$ tar -tzvf mydocs.tar.gz        # просто посмотрели
drwxr-xr-x student/student   0 2025-06-10 13:04 mydocs/
-rw-rw-r-- student/student  17 2025-06-10 13:04 mydocs/a.txt
-rw-rw-r-- student/student  17 2025-06-10 13:04 mydocs/b.txt
$ mkdir restored
$ tar -xzvf mydocs.tar.gz -C restored/
mydocs/
mydocs/a.txt
mydocs/b.txt
$ ls restored/mydocs/
a.txt  b.txt`,
        },
        {
          title: 'Спросите у системы',
          difficulty: 2,
          description:
            'Не гугля, узнайте у самой Ubuntu: что делает флаг `-h` у команды `ls` в сочетании с `-l`? Используйте и `man ls` (поиск внутри — клавиша `/`, выход — `q`), и короткую справку `ls --help`. Запишите ответ в файл `answer.txt`.',
          hint: 'В `ls --help` можно найти нужную строку конвейером с grep: ищите слово «human».',
          solution: `$ ls --help | grep -- -h
  -h, --human-readable      with -l and -s, print sizes like 1K 234M 2G
$ echo "флаг -h делает размеры читаемыми: 1K, 234M, 2G" > answer.txt
$ cat answer.txt
флаг -h делает размеры читаемыми: 1K, 234M, 2G
# то же нашли бы в man ls поиском по /human`,
        },
        {
          title: 'Соберите досье на самого себя',
          difficulty: 3,
          description:
            'Сохраните в файл `my-session.txt` последние 10 команд из вашей истории, а в файл `tar-commands.txt` — все команды из истории, где встречалось слово `tar`. Выведите оба файла на экран.',
          hint: 'История сама по себе — обычный текстовый вывод: его можно резать `tail`, фильтровать `grep` и сохранять через `>`.',
          solution: `$ history | tail -10 > my-session.txt
$ history | grep tar > tar-commands.txt
$ cat my-session.txt
  503  tar -czvf mydocs.tar.gz mydocs/
  504  ls -lh mydocs.tar.gz
# ...и так далее, ваши номера и команды будут свои
$ cat tar-commands.txt
  503  tar -czvf mydocs.tar.gz mydocs/
  510  tar -tzvf mydocs.tar.gz
  512  tar -xzvf mydocs.tar.gz -C restored/`,
        },
      ],
      quiz: [
        {
          question: 'Чем отличаются `tar -czvf backup.tar.gz папка/` и `tar -xzvf backup.tar.gz`?',
          options: [
            'Первая создаёт архив, вторая распаковывает его',
            'Первая сжимает сильнее, чем вторая',
            'Первая работает от root, вторая — от обычного пользователя',
            'Ничем, это два синонима',
          ],
          correctIndex: 0,
          explanation:
            'Буква `c` (create) создаёт архив, `x` (extract) — извлекает из него файлы. Остальные флаги те же: `z` — gzip-сжатие, `v` — показ процесса, `f` — имя файла.',
        },
        {
          question: 'Как выйти из справочной страницы man?',
          options: [
            'Ctrl+C',
            'Напечатать exit',
            'Клавиша q',
            'Закрыть окно терминала',
          ],
          correctIndex: 2,
          explanation:
            'man показывает текст через программу-пейджер (less), из которой выходят клавишей q (quit). Кстати, так же выходили из less в модуле 2.',
        },
        {
          question: 'Что делает команда `history | grep ssh`?',
          options: [
            'Подключается по ssh к серверу history',
            'Показывает все команды из вашей истории, содержащие слово ssh',
            'Удаляет из истории команды с ssh',
            'Показывает историю подключений других пользователей',
          ],
          correctIndex: 1,
          explanation:
            'history выводит список прошлых команд, а grep оставляет только строки со словом ssh. Классическая связка «что я там вводил вчера?».',
        },
        {
          question: 'В команде `tar -czvf a.tar.gz docs/` почему буква f стоит последней в группе флагов?',
          options: [
            'Это требование синтаксиса bash, а не tar',
            'Потому что сразу после f должно идти имя файла архива',
            'Случайность: порядок букв не важен',
            'f в середине отключит сжатие gzip',
          ],
          correctIndex: 1,
          explanation:
            'Флаг `-f` принимает аргумент — имя файла, которое идёт следующим словом. Если f окажется в середине, tar примет за имя файла соседнюю букву и всё сломается.',
        },
      ],
    },
    {
      id: 'm03-l05',
      title: 'Переменные окружения и ваш ~/.bashrc',
      minutes: 30,
      intro:
        'Научитесь настраивать терминал под себя: читать и создавать переменные окружения, управлять списком поиска команд PATH и сохранять свои настройки навсегда в файле ~/.bashrc. Больше никакого «почему после перезахода всё пропало?».',
      blocks: [
        {
          type: 'paragraph',
          text: 'В прошлых уроках вы научились перенаправлять потоки, искать текст и файлы, работать с архивами. Терминал уже слушается вас — но пока вы живёте в нём «как в гостях»: все настройки стандартные, а любые изменения исчезают после закрытия окна. Сегодня сделаем терминал по-настоящему вашим и заодно разгадаем загадку: почему установленная переменная «живёт до перезахода», а настройки системы — вечно.',
        },
        {
          type: 'callout',
          variant: 'simple',
          text: 'Представьте комнату, на стене которой висят записки: «моё имя — Студент», «мои вещи в шкафу Downloads», «я говорю по-русски». Любая программа, заходящая в комнату, может прочитать эти записки и учесть их в работе. **Переменные окружения** (environment variables) — это и есть такие записки на стене вашего терминала: пары «имя = значение», доступные каждой запущенной команде.',
        },
        { type: 'heading', text: 'Что такое переменные окружения' },
        {
          type: 'paragraph',
          text: 'Переменная окружения — это именованное значение, которое оболочка **bash** передаёт программам. Обратиться к значению можно, поставив перед именем знак доллара: `$HOME` — путь к вашей домашней папке, `$USER` — имя текущего пользователя, `$LANG` — язык интерфейса. Увидеть весь список помогут команды `printenv` или `env` — они выводят все «записки на стене» разом.',
        },
        {
          type: 'code',
          title: 'Знакомимся с переменными',
          code: `$ echo $HOME            # где моя домашняя папка
/home/student
$ echo $USER            # как меня зовут
student
$ echo $LANG            # на каком языке говорит система
ru_RU.UTF-8
$ printenv | head -6    # первые шесть переменных из полного списка
SHELL=/bin/bash
PWD=/home/student
LOGNAME=student
HOME=/home/student
LANG=ru_RU.UTF-8
TERM=xterm-256color`,
        },
        { type: 'heading', text: 'PATH: где терминал ищет команды' },
        {
          type: 'paragraph',
          text: 'Самая важная переменная — **PATH**. Замечали, что `python3` или `grep` запускаются просто по имени, без полного пути, а ваш собственный скрипт из текущей папки — только как `./script.sh`? Секрет в том, что PATH — это список папок, разделённых двоеточиями. Когда вы вводите команду, bash по очереди заглядывает в каждую папку из списка и ищет там файл с таким именем. Текущей папки (`.`) в списке нет — поэтому и нужен `./`. Это сделано намеренно, ради безопасности: иначе чужой скрипт с именем `ls`, случайно попавший в вашу папку, запускался бы вместо настоящего `ls`.',
        },
        {
          type: 'code',
          title: 'Смотрим PATH и временно его меняем',
          code: `$ echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
$ which python3          # в какой папке из PATH нашёлся python3
/usr/bin/python3
$ mkdir ~/bin            # своя папка для скриптов
$ export PATH="$PATH:$HOME/bin"   # добавить её в конец списка
$ echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin:/home/student/bin`,
        },
        {
          type: 'callout',
          variant: 'remember',
          text: 'Команда `export` делает переменную видимой для всех программ, которые вы запустите из этого терминала. Но действует она **только до закрытия терминала**: закроете окно — и bash забудет ваше изменение PATH. Вот и ответ на «почему после перезахода всё пропало». Как сделать настройку постоянной — узнаем через минуту, когда дойдём до `~/.bashrc`.',
        },
        {
          type: 'paragraph',
          text: 'Свои переменные создаются тем же `export` — и есть два режима. Запись `EDITOR=nano nano diary.txt` задаёт переменную **только для одной этой команды**: редактор nano запустится со значением EDITOR=nano, а после завершения переменной снова не существует. А `export EDITOR=nano` действует **на всю сессию** терминала. Переменная `EDITOR` говорит программам, каким редактором вы предпочитаете пользоваться. Вспомните: когда в модуле 9 вы будете настраивать планировщик `crontab`, система спросит, какой редактор открыть, и предложит выбрать руками из списка. С установленным `EDITOR` она возьмёт ваш любимый редактор автоматически, без лишних вопросов.',
        },
        {
          type: 'code',
          title: 'Одноразово и на всю сессию',
          code: `$ EDITOR=nano nano diary.txt   # переменная живёт, пока работает эта команда
$ echo $EDITOR                  # после завершения — пусто

$ export EDITOR=nano            # а теперь — на всю сессию
$ echo $EDITOR
nano`,
        },
        { type: 'heading', text: '~/.bashrc: сценарий приветствия' },
        {
          type: 'paragraph',
          text: 'Файл `~/.bashrc` (читается «bash-эр-си», rc — от run commands) — это **сценарий приветствия**: bash выполняет его каждый раз, когда вы открываете новый терминал. Всё, что туда записано, применяется автоматически — вот почему постоянные настройки кладут именно сюда. Загляните внутрь: Ubuntu уже добавила туда кое-что полезное, например **псевдонимы** (alias) — короткие имена для длинных команд.',
        },
        {
          type: 'code',
          title: 'Заглядываем в ~/.bashrc и добавляем своё',
          code: `$ cp ~/.bashrc ~/.bashrc.bak     # страховка: резервная копия
$ grep -n "alias" ~/.bashrc      # какие псевдонимы уже есть
87:    alias ls='ls --color=auto'
90:    alias grep='grep --color=auto'
98:# some more ls aliases
99:alias ll='ls -alF'
100:alias la='ls -A'
101:alias l='ls -CF'
$ nano ~/.bashrc                 # допишите в конец файла две строки:
#   alias cls='clear'
#   export PATH="$PATH:$HOME/bin"
$ source ~/.bashrc               # применить изменения прямо сейчас
$ cls                            # экран очищается — псевдоним работает!`,
        },
        {
          type: 'callout',
          variant: 'warning',
          text: 'Правило безопасности: **дописывайте своё в конец файла, ничего не удаляя из существующего содержимого** — там живут настройки, без которых терминал будет выглядеть и вести себя иначе. И всегда делайте копию `cp ~/.bashrc ~/.bashrc.bak`: если что-то пойдёт не так, откатитесь командой `cp ~/.bashrc.bak ~/.bashrc`.',
        },
        {
          type: 'paragraph',
          text: '**Бонус для любопытных.** Строка приглашения — тот самый текст `student@ubuntu:~$` перед курсором — тоже настраивается в `~/.bashrc`. За неё отвечает переменная **PS1**: в ней закодированы имя пользователя, имя компьютера и текущая папка. Синтаксис PS1 выглядит заклинанием вроде `\\u@\\h:\\w\\$`, поэтому пока просто запомните, где она живёт, — когда захотите цветное приглашение, вы будете знать, куда идти.',
        },
        { type: 'heading', text: 'Что дальше' },
        {
          type: 'paragraph',
          text: 'Модуль «Терминал на полную мощность» пройден целиком: потоки и конвейеры, grep, find и архивы, а теперь ещё и окружение с `~/.bashrc` — вы уверенный **Пользователь**. В следующем модуле «Пользователи и права» выясним, кто в системе главный: зачем нужен `sudo`, что означают загадочные буквы `rwx` в выводе `ls -l` и почему заглянуть в `/root` у обычного пользователя не получится.',
        },
      ],
      tasks: [
        {
          title: 'Изучите своё окружение',
          difficulty: 1,
          description:
            'Выведите значения трёх переменных: вашу домашнюю папку, имя пользователя и язык системы. Затем найдите в полном списке переменных все строки, где упоминается ваше имя пользователя.',
          hint: 'Значение переменной показывает `echo $ИМЯ`. Полный список даёт `printenv`, а отфильтрует его старый знакомый `grep` в конвейере.',
          solution: `$ echo $HOME
/home/student
$ echo $USER
student
$ echo $LANG
ru_RU.UTF-8
$ printenv | grep student
PWD=/home/student
LOGNAME=student
USER=student
HOME=/home/student`,
        },
        {
          title: 'Создайте псевдонимы на одну сессию',
          difficulty: 2,
          description:
            'Не трогая файлы настроек, создайте два псевдонима прямо в терминале: `cls` для очистки экрана (команда `clear`) и `docs` для перехода в папку `~/Documents`. Проверьте оба, а затем выведите список всех псевдонимов сессии командой `alias`.',
          hint: 'Синтаксис: `alias имя=\'команда\'`. Обратите внимание на одинарные кавычки и на то, что вокруг знака равенства пробелов нет.',
          solution: `$ alias cls='clear'
$ alias docs='cd ~/Documents'
$ cls                       # экран очистился
$ docs                      # перешли в Documents
$ pwd
/home/student/Documents
$ cd                        # вернёмся домой
$ alias | grep -E 'cls|docs'
alias cls='clear'
alias docs='cd ~/Documents'`,
        },
        {
          title: 'Добавьте ~/bin в PATH навсегда',
          difficulty: 3,
          description:
            'Сделайте так, чтобы папка `~/bin` добавлялась в PATH при каждом открытии терминала: создайте папку, сделайте резервную копию `~/.bashrc`, допишите в конец файла строку `export PATH="$PATH:$HOME/bin"` и примените изменения без перезапуска терминала. Убедитесь, что папка появилась в конце PATH.',
          hint: 'Четыре шага: `mkdir`, `cp` для страховки, `nano` для правки, `source` для применения. Проверка — `echo $PATH`.',
          solution: `$ mkdir ~/bin
$ cp ~/.bashrc ~/.bashrc.bak          # страховка
$ nano ~/.bashrc                      # допишите в конец строку:
# export PATH="$PATH:$HOME/bin"
$ source ~/.bashrc                    # применить сразу
$ echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin:/home/student/bin`,
        },
        {
          title: 'Расследование: куда пропала переменная?',
          difficulty: 3,
          description:
            'Создайте переменную `MY_NAME` со своим именем через `export` и убедитесь, что она видна. Теперь запустите новый экземпляр bash (просто командой `bash`) — это «свежий» терминал внутри текущего. Проверьте переменную там, выйдите обратно командой `exit` и объясните себе результат: почему в новом bash переменной не оказалось и что нужно сделать, чтобы она появлялась всегда?',
          hint: 'Проверка значения — `echo $MY_NAME`. Подумайте: какой файл читает каждый новый bash при запуске?',
          solution: `$ export MY_NAME="Student"
$ echo $MY_NAME
Student
$ bash                  # новый "терминал внутри терминала"
$ echo $MY_NAME
Student
$ exit                  # вернулись обратно
# внимание: export всё-таки передал переменную потомку!
# а вот если бы мы написали MY_NAME="Student" БЕЗ export,
# в новом bash было бы пусто.
# Но в любом случае после ЗАКРЫТИЯ терминала переменная умрёт.
# Чтобы она появлялась всегда — допишите export в ~/.bashrc:
$ nano ~/.bashrc        # добавить строку: export MY_NAME="Student"`,
        },
      ],
      quiz: [
        {
          question: 'Что выведет команда `echo $HOME` у пользователя student?',
          options: [
            '/home/student',
            'Слово HOME',
            '$HOME',
            'student',
          ],
          correctIndex: 0,
          explanation:
            'Знак доллара перед именем означает «подставь значение переменной». HOME хранит путь к домашней папке пользователя — для student это /home/student.',
        },
        {
          question: 'Почему команда `python3` запускается без полного пути, а скрипт из текущей папки требует `./script.sh`?',
          options: [
            'python3 — встроенная команда bash, а скрипты встроенными не бывают',
            'Скрипты требуют ./ только если у них нет прав на выполнение',
            'Папка с python3 есть в переменной PATH, а текущей папки в PATH нет — ради безопасности',
            'Это случайное ограничение конкретной версии Ubuntu',
          ],
          correctIndex: 2,
          explanation:
            'bash ищет команды в папках из PATH, и /usr/bin (где живёт python3) в списке есть. Текущая папка в PATH не входит намеренно: иначе чужой файл с именем известной команды мог бы запуститься вместо неё.',
        },
        {
          question: 'Вы выполнили `export EDITOR=nano`. Как долго будет жить эта переменная?',
          options: [
            'Вечно, пока её не удалят из системы',
            'Только во время выполнения следующей команды',
            'Ровно 24 часа',
            'До конца текущей сессии терминала — закроете окно, и переменная исчезнет',
          ],
          correctIndex: 3,
          explanation:
            'export действует только в рамках текущего экземпляра bash. Чтобы переменная появлялась при каждом открытии терминала, строку export нужно дописать в ~/.bashrc.',
        },
        {
          question: 'Вы отредактировали `~/.bashrc`. Как применить изменения, не закрывая терминал?',
          options: [
            'Командой `apply ~/.bashrc`',
            'Командой `source ~/.bashrc`',
            'Командой `reload bash`',
            'Никак: придётся перезагружать компьютер',
          ],
          correctIndex: 1,
          explanation:
            '`source` (или сокращённо просто точка: `. ~/.bashrc`) выполняет файл в текущей сессии bash — все alias и export из него применяются немедленно. Команд apply и reload для этого не существует.',
        },
        {
          question: 'Как правильно добавить свою строку в `~/.bashrc`, чтобы ничего не сломать?',
          options: [
            'Стереть старое содержимое и записать только свои настройки',
            'Вставить строку в самое начало файла, чтобы она выполнилась первой',
            'Дописать свою строку в конец файла, предварительно сделав копию `cp ~/.bashrc ~/.bashrc.bak`',
            'Создать рядом файл bashrc.new — bash сам разберётся',
          ],
          correctIndex: 2,
          explanation:
            'Существующее содержимое ~/.bashrc — это рабочие настройки Ubuntu, удалять их нельзя. Свои строки дописывают в конец, а резервная копия позволит откатиться одной командой `cp ~/.bashrc.bak ~/.bashrc`.',
        },
      ],
    },
  ],
};
